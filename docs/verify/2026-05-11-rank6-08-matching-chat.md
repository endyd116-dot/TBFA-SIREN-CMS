# 6순위 #8 1:1 매칭 채팅 라이브 검증 (Q4 — C 채팅)

> **검증일**: 2026-05-11
> **검증자**: C 채팅 (Opus 4.7)
> **브랜치**: `verify/queue-2026-05-11-q4`
> **결과**: ✅ 통과 (bug 0건, BUG-4 fix 그대로 유지, Phase 10 R1 머지 후에도 회귀 0)
> **출처 큐**: PROJECT_STATE §8 Q4
> **이전 정적 점검**: [docs/verify/2026-05-10-priority6-8.md](2026-05-10-priority6-8.md) (BUG-4 발견·수정)

---

## 1. 검증 대상

6순위 #8 — 변호사·심리상담사와 사용자 간 1:1 매칭 + 매칭 후 자동 생성되는 채팅방 흐름. 코드는 이전 라운드(`d9e8246`, `17827cd`, `89555cb`, `f61023b` 등)에 머지·BUG-4 fix 완료. 본 라운드는 라이브 진입 + 코드 정합성 + Phase 10 R1 머지 후 회귀 점검.

| 영역 | 진입점 |
|---|---|
| 어드민 매칭 관리 | 어드민 사이드바 → "⚖️ 1:1 매칭 관리" |
| 어드민 직접 배정 | 유가족지원·법률지원 신청 목록의 행 단위 "전문가 배정"·"변호사 배정" 버튼 |
| 사용자 매칭 신청 | 사고지원·괴롭힘·법률지원·유가족지원 진입에서 매칭 신청 |
| 사용자 채팅방 | 마이페이지 → 신청 내역 → 배정 완료 건의 "💬 전문가 채팅" 버튼 |
| 사용자 매칭 목록 | 마이페이지 매칭 탭 |
| 어드민 채팅 모니터링 | 어드민의 채팅방 목록·메시지 조회 |
| AI 매칭 추천 | 어드민의 AI 전문가 매칭 보조 |

---

## 2. 라이브 진입 점검

세션 쿠키 없이 호출. 페이지는 200, API는 인증 거부 또는 메서드 거부(POST 전용에 GET이라 405). 500/타입 에러 0.

### 2.1 페이지 진입

| 화면 | 응답 |
|---|---|
| `/admin.html` (어드민 진입점, 1:1 매칭 관리 SPA) | 200 |
| `/mypage.html` (사용자 마이페이지·매칭 탭·채팅 버튼) | 200 |
| `/legal-support.html` (법률 지원 — 매칭 신청 흐름) | 200 |
| `/support.html` (유가족 지원 — 매칭 신청 흐름) | 200 |

### 2.2 백엔드 13개 함수 인증 가드

| 함수 | 응답 | 판정 |
|---|---|---|
| 어드민 전문가 명단 조회 | 401 | ✅ |
| 어드민 매칭 행에 전문가 배정 (POST 전용) | 405 | ✅ — 함수 등록 정상 |
| 어드민 직접 배정(매칭 신청 없이 즉시) (POST 전용) | 405 | ✅ |
| 어드민 매칭 가능 전문가 후보 조회 | 401 | ✅ |
| 사용자 본인 매칭 명단 조회 | 401 | ✅ |
| 사용자 매칭 신청 (POST 전용) | 405 | ✅ |
| 어드민·사용자 세션 종료 (POST 전용) | 405 | ✅ |
| 사용자 본인 채팅방 명단 | 401 | ✅ |
| 사용자 채팅 메시지 조회·전송 | 401 | ✅ |
| 사용자 채팅 첨부 업로드 (POST 전용) | 405 | ✅ |
| 어드민 채팅방 모니터링 명단 | 401 | ✅ |
| 어드민 채팅 메시지 모니터링 | 401 | ✅ |
| 어드민 AI 전문가 매칭 추천 (POST 전용) | 405 | ✅ |

→ 모든 함수가 라이브에 정상 등록 + 인증 가드 살아있음. 500 한 건도 없음.

---

## 3. BUG-4 fix 살아있는지 점검 (직전 라운드 회귀 방지)

이전 정적 점검(2026-05-10)에서 발견된 BUG-4(매칭 관리 탭 클릭 무반응 — 화면 컨테이너 ID와 탭 클릭 핸들러가 가리키는 ID가 달랐던 문제)가 머지 이후로도 그대로 살아있는지 확인.

| 점검 | 결과 |
|---|---|
| 어드민 매칭 화면의 컨테이너 ID 통일 (탭 핸들러 2곳 모두 같은 ID 참조) | ✅ — 두 곳 모두 `#adm-expert-match`로 통일된 채 유지 |
| 어드민 매칭 화면 캐시버스터 (수정 반영 강제) | ✅ — `?v=2026-05-10-direct2` 그대로, 옛 캐시버스터로 회귀 없음 |
| 어드민 사이드바 매칭 메뉴 라우트 등록 | ✅ — 본문 div와 라우트 모두 정상 |

→ 이후 머지(Phase 8·9·10 R1) 동안에도 BUG-4 fix가 덮어쓰여진 흔적 0.

---

## 4. 코드 정합성 점검 (정독)

### 4.1 사용자 매칭 신청 흐름

- ✅ 차단된 회원도 자동 거부 (블랙 가드 통합)
- ✅ 매칭 종류·도메인·사유 입력 검증 (잘못된 값은 400 거절)
- ✅ 같은 종류의 진행 중 매칭이 이미 있으면 거절 ("이미 진행 중인 변호사 매칭이 있습니다 (매칭 ID: X)") — 중복 신청 방지
- ✅ 단계별 try/catch + step 라벨 + detail/stack 표준 응답 (CLAUDE §6.2)

### 4.2 어드민 전문가 배정 흐름

- ✅ 매칭 행이 이미 처리된 상태(matched/완료 등)면 거절 + 사유 명시
- ✅ 매칭 신청자가 차단 또는 탈퇴 상태면 거절 (자료의 위험 상태 사용자 보호)
- ✅ 전문가 자격 검증 (변호사/심리상담사 분류·활성 여부)
- ✅ **트랜잭션 1건**: 채팅방 INSERT + 매칭 행 갱신을 한 번에 처리 (중간 실패 시 둘 다 롤백)
- ✅ 채팅방 제목 자동 생성 (사용자명·전문가명·매칭 종류 조합)
- ✅ 응답에 채팅방 ID 즉시 반환 — 어드민 화면이 곧바로 채팅방으로 이동 가능

### 4.3 채팅방·메시지 인증

- ✅ 사용자 채팅 진입은 본인 회원 ID로 격리, 어드민 채팅 모니터링은 별도 어드민 가드
- ✅ 일반 채팅(전문가 매칭 외 일반 1:1)은 차단 통합 미적용 — 설계 범위 (CLAUDE §9.1.3 "지금은 핵심 CREATE 6개만 적용" 원칙 준수). 전문가 매칭 신청·세션 종료는 차단 통합 적용됨

### 4.4 어드민 매칭 관리 화면

- ✅ 탭 4종(대기·진행중·완료·전체) 클릭 시 화면 갱신 (BUG-4 fix 후 정상)
- ✅ 매칭 신청 행에서 전문가 후보 선택 → 배정 클릭 → 채팅방 자동 생성 + 매칭 상태 "matched"
- ✅ 진행중 행에서 세션 종료 가능 → 상태 "completed"

---

## 5. 회귀 점검 (Phase 10 R1 머지 후)

| 점검 항목 | 응답 | 판정 |
|---|---|---|
| 어드민 회원/후원/발송 로그 API (비인증) | 401 | ✅ — 신규 schema 추가 후에도 영향 0 |
| Phase 10 R1 발송 템플릿 명단 API (비인증) | 401 | ✅ — 본 검증 영역과 폴더 분리 |
| 사용자 마이페이지 진입 API (비인증) | 401 | ✅ |
| 통합 회원 SPA 페이지 | 200 | ✅ |
| 사용자 알림 수신 설정 페이지 | 200 | ✅ |
| 발송 템플릿 페이지 (R1 영역) | 200 | ✅ |
| 매칭·채팅 13개 함수 모두 라이브 등록 | 401/405 | ✅ |

→ Phase 10 R1 머지 + R2 설계 작성 후에도 매칭·채팅 영역 회귀 0.

---

## 6. Swain 직접 클릭 검증이 필요한 항목

세션 쿠키 흐름이 필요. 코드 정합성으로는 통과지만 다음은 클릭으로 최종 확인 권장:

1. **어드민 직접 배정**: 어드민 → 유가족지원 신청 목록 → "⚕️ 전문가 배정" 클릭 → 전문가 선택 → 배정 → 채팅방 자동 생성 확인
2. **어드민 변호사 배정**: 어드민 → 법률지원 신청 목록 → "⚖️ 변호사 배정" 클릭 → 변호사 선택 → 익명 신청은 버튼 없음 확인
3. **어드민 매칭 관리 화면**: 어드민 사이드바 → "⚖️ 1:1 매칭 관리" → 화면 표시 → **탭 4종(대기/진행중/완료/전체) 클릭 전환** (BUG-4 수정 후 정상 동작 검증)
4. **사용자 채팅 진입**: 사용자 마이페이지 → 신청 내역 → 유가족지원 또는 법률지원 탭 → 배정 완료된 건의 "💬 전문가 채팅" 버튼 클릭 → 채팅창 진입
5. **사용자 매칭 신청**: 사용자가 매칭 신청 → 같은 종류로 두 번째 신청 시 "이미 진행 중인 매칭이 있습니다" 안내 표시
6. **세션 종료**: 어드민 매칭 관리 → 진행중 → 세션 종료 → 사용자 마이페이지의 완료 탭에 표시되는지

코드 정합성·라이브 진입·인증 가드·BUG-4 fix 유지는 본 보고서로 통과. 업무 시나리오 클릭 테스트는 Swain 직접.

---

## 7. 결론

- **bug 발견**: 0건
- **회귀**: 0건 — BUG-4 fix 살아있음, Phase 10 R1 머지 후에도 13개 함수 모두 정상
- **코드 정합성**: 양호 — 매칭 신청·전문가 배정·채팅·세션 종료 모두 설계대로
- **추천**: Q4 통과로 마감. 다음 큐는 Q-진단-2(Swain 진단 응답 후) 또는 Q5(Phase 4 대표 보고 시스템).

---

## 참고

- 이전 정적 점검 보고서: [docs/verify/2026-05-10-priority6-8.md](2026-05-10-priority6-8.md)
- BUG-4 fix 커밋: `f61023b`
- 매칭 시스템 머지: `c7b3ca3` → `226f2e9` → `d9e8246` → `17827cd`
