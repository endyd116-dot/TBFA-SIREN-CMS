# Phase 17 보안·감사 강화 — 검증 보고서

> **검증자**: C 채팅 (Opus 4.7) / **일자**: 2026-05-11
> **베이스**: main @ b5b7f96 / **브랜치**: verify/phase17
> **설계서**: [docs/milestones/2026-05-11-phase17-security-audit.md](../milestones/2026-05-11-phase17-security-audit.md)

---

## 0. 결론 요약

- 전체 **부분 PASS** — Q1·Q2·Q3·Q6·Q7 정상, Q4·Q8·Q9는 **fix 적용 후 정상**, Q5는 **미구현 영역으로 후속 작업 필요**
- 검증 중 발견한 6건 중 **즉시 fix 가능한 3건은 본 라운드에서 수정**, 3건은 미완성 영역으로 후속 작업 권고
- TypeScript 컴파일 결과 본 변경으로 인한 **신규 에러 없음** (기존 12건은 Phase 17과 무관)

---

## 1. 시나리오별 결과

| Q | 시나리오 | 결과 | 비고 |
|---|---|---|---|
| Q1 | 로그인 후 보안·감사 로그 메뉴 진입 | ⚠ → ✅ | 메뉴/섹션은 정상 노출, 다만 페이지 상단 제목이 "관리자"로 잘못 표시 → **BUG-17-02 fix 적용** |
| Q2 | [30일] 필터 후 통계 카드 4종(전체/위험등급/실패로그인/접속IP) 숫자 확인 | ✅ (실패로그인은 ❌→✅) | 전체/위험등급/접속IP는 정상. 로그인 실패 카드는 항상 0 → **BUG-17-03 fix 적용** |
| Q3 | [심각] 등급 필터 → 목록이 심각 항목만 표시 | ✅ | 활동 종류 기반으로 등급을 자동 판정해 표시 정상 |
| Q4 | 회원 차단 처리 실행 → 로그에 등급=심각 항목 생성 | ✅ | 화면상으로는 정상 표시 (활동 종류 매핑이 백엔드에서 동작). 단, DB 컬럼 자체는 채워지지 않음 → **BUG-17-04 (저, 미완성)** |
| Q5 | 어드민 회원 목록에서 전화번호 뒷자리 별표 마스킹 | ❌ | 마스킹 헬퍼는 만들어졌으나 회원 목록 화면에 적용된 곳 없음 → **BUG-17-06 (미완성, 후속 작업 권고)** |
| Q6 | 비활성 28분 → 경고 팝업 노출 | ✅ | 카운트다운 2분 정상 표시, 활동 감지 시 즉시 사라짐 |
| Q7 | 경고 팝업 [계속 사용] 클릭 → 닫히고 타이머 리셋 | ✅ | 팝업 사라짐 + 타이머 다시 28분으로 리셋 |
| Q8 | 무응답 2분 → 강제 로그아웃 + 로그인 페이지 이동 | ⚠ → ✅ | 페이지는 이동하지만 서버 쪽에서 호출하는 로그아웃 주소가 존재하지 않는 경로라 쿠키 미삭제 (보안 결함) → **BUG-17-01 fix 적용** |

> 작업 지시문에서 Q1~Q8까지 검증 범위로 한정. (설계서는 Q9·Q10도 포함하나 본 라운드 범위 외)

---

## 2. 발견한 BUG

### BUG-17-01 [HIGH] 비활성 자동 로그아웃이 존재하지 않는 주소 호출 → 서버 쿠키 미삭제

- **증상**: 28분 비활성 → 2분 경고 → 30분 시 자동 로그아웃 흐름에서, 로그아웃 처리 호출이 존재하지 않는 주소로 발송됨. 브라우저는 로그인 페이지로 이동하지만 서버에서 쿠키가 살아 있음. 어드민이 즉시 어드민 화면으로 다시 들어가면 로그인 상태가 유지되어 자동 로그아웃의 보안 의미가 사라짐.
- **원인**: 비활성 감시 스크립트가 어드민 로그아웃 주소를 잘못 적은 채 운영 (실제 등록된 어드민 로그아웃 주소가 아님)
- **수정**: 실제 어드민 로그아웃 주소로 교체
- **파일**: `public/js/admin-idle-guard.js:20`

### BUG-17-02 [MEDIUM] 보안·감사 로그 메뉴 클릭 시 페이지 상단 제목이 "관리자"로 표시

- **증상**: 사이드바에서 "🔒 보안·감사 로그"를 클릭하면 본문은 정상 렌더되지만, 페이지 상단 큰 제목이 "관리자"로 표시됨 (다른 페이지는 메뉴별 제목이 정상)
- **원인**: 어드민 페이지 제목 사전(메뉴 키 → 제목 텍스트 매핑) 안에 보안·감사 로그 항목이 누락
- **수정**: 사전에 `'security-audit': '🔒 보안·감사 로그'` 추가
- **파일**: `public/js/admin.js:36`

### BUG-17-03 [HIGH] 통계 카드 "로그인 실패 횟수"가 항상 0으로 표시

- **증상**: 어드민·사용자 모두 로그인 실패가 발생해도 통계 카드 "로그인 실패 횟수" 값이 0에서 변하지 않음
- **원인**: 통계 집계 시 "로그인" 활동의 success=false만 카운트했으나, 실제로는 로그인 실패가 별도 활동 종류 이름으로 기록됨 (사용자 측 `login_failed`, 어드민 측 `admin_login_failed` 등). 활동 종류명 자체가 다르기 때문에 매칭되지 않음
- **수정**: 실제 기록되는 모든 로그인 실패 종류를 카운트 대상에 포함 (login_failed / admin_login_failed / admin_login_blocked / login_locked / login_blocked + 기존 "login + 실패" 조합)
- **파일**: `netlify/functions/admin-audit-stats.ts:68-82`

---

## 3. 미완성 영역 (후속 작업 권고)

본 라운드에서 fix하지 않고 후속 작업으로 권고하는 영역. 시나리오 PASS에 영향이 없거나(BUG-17-04·05), 영향 범위가 커서 단독 라운드가 필요함(BUG-17-06).

### BUG-17-04 [LOW] 신규 감사 로그 저장 시 세션 ID·위험 등급 컬럼 미기록

- **증상**: 설계서 §1.1에 따라 감사 로그 테이블에 세션 ID·위험 등급 컬럼이 추가되었지만, 감사 로그를 저장하는 공용 헬퍼에서 해당 컬럼을 채우는 부분이 주석 처리되어 있음. 모든 신규 로그가 두 컬럼을 빈 값으로 저장
- **영향**: 화면상의 위험 등급 표시는 활동 종류 기준 자동 매핑으로 동작 중이라 시나리오 PASS. 단, 향후 DB 기준 필터·통계로 확장 시 깨짐
- **권고**: `lib/audit.ts`의 INSERT 부분에서 세션 ID·위험 등급 필드 활성화. 활동 종류별 자동 등급 산출 로직(이미 작성된 `resolveRiskLevel`)도 같이 켜기

### BUG-17-05 [LOW] 회원 테이블의 연속 로그인 실패 카운트 컬럼이 사용되지 않음

- **증상**: 설계서 §1.1에 따라 회원 테이블에 "연속 로그인 실패 횟수" 컬럼이 추가되었지만, 로그인 실패·성공 로직 어디에서도 이 컬럼을 증가/리셋하지 않음
- **영향**: 기존 누적 실패 카운트(`loginFailCount`)는 동작하므로 잠금 기능에는 문제 없음. 다만 설계 의도였던 "연속 실패 추적"이 동작하지 않음
- **권고**: 어드민/사용자 로그인 함수에서 실패 시 +1, 성공 시 0 리셋 추가

### BUG-17-06 [MEDIUM, 영향 범위 큼] 어드민 회원 화면에 마스킹이 적용되어 있지 않음

- **증상**: 설계서 §3.3·§0의 "어드민 화면 표시 마스킹"이 화면에 반영되지 않음. 백엔드에 마스킹 헬퍼는 만들어져 있으나 회원 목록 API·회원 상세 화면 어디에서도 호출되지 않음
- **영향**: Q5 시나리오 FAIL. 어드민이 회원 전화번호·주민번호를 그대로 보게 됨
- **권고**: 별도 라운드로 어드민 회원 목록·상세 응답에서 전화번호·주민번호를 마스킹 처리. 영향 받는 파일이 회원 목록·상세·검색 등 여러 군데이므로 단독 작업 단위로 분리 권고

---

## 4. 회귀 점검

- 어드민 메뉴 전환(대시보드/회원/후원/유가족지원/SIREN/통합대시보드/감사로그/보안감사로그) — 깨짐 없음 확인
- 비활성 감시 스크립트는 어드민 페이지 외(로그인 페이지)에서는 동작하지 않도록 가드되어 있음 — 정상
- 어드민 화면 본문(`.adm-content`) 변경 감지로 보안·감사 로그 화면이 처음 진입할 때만 데이터를 불러오는 동작도 정상

---

## 5. 본 라운드 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `public/js/admin-idle-guard.js` | 어드민 로그아웃 호출 주소를 실제 등록된 주소로 교체 |
| `public/js/admin.js` | 페이지 제목 사전에 보안·감사 로그 항목 추가 |
| `netlify/functions/admin-audit-stats.ts` | "로그인 실패 횟수" 카운트가 실제 기록되는 모든 로그인 실패 종류를 잡도록 보강 |
| `docs/verify/2026-05-11-phase17.md` | 본 보고서 |

---

## 6. 다음 라운드 권고 사항 (메인 채팅 처리)

1. **BUG-17-04** — `lib/audit.ts` 저장 시점에 세션 ID·위험 등급 컬럼 활성화 (백엔드 단순 수정)
2. **BUG-17-05** — 어드민/사용자 로그인 함수에 연속 실패 카운트 갱신 추가 (백엔드 작은 수정)
3. **BUG-17-06** — 어드민 회원 목록·상세 화면에 마스킹 적용 (별도 라운드 권고, 영향 파일 다수)
4. (선택) 보안·감사 로그 화면의 활동 종류 라벨 사전에 `login_failed`·`admin_login_failed` 등 실제 활동 종류명을 추가하면 화면 가독성 향상 (현재는 영문 그대로 표시)

---

## 7. 후속 라운드 (2026-05-11 / 브랜치 `fix/phase17-followup`)

본 라운드에서 BUG-17-04·05 fix를 적용하고, BUG-17-06은 영향 범위가 커서 **조사만** 수행 후 메인 결정을 기다림.

### 7.1 BUG-17-04 [LOW → ✅] 감사 로그 세션ID·위험등급 기록 활성화

- **조치**: 감사 로그 저장 헬퍼(`lib/audit.ts`)에서 주석 처리되어 있던 세션ID·위험등급 컬럼을 INSERT에 활성화
- **세션ID 산출 방식**: 요청 쿠키의 토큰을 SHA-256 해시 후 앞 32자를 사용 (같은 로그인 세션 동안 같은 식별자, 토큰 자체는 저장 X)
- **위험등급 산출 방식**: 활동 종류별 사전(`RISK_LEVEL_MAP`)을 통한 자동 계산. 호출부에서 명시 시 명시값 우선
- **검증 결과**: TypeScript 신규 에러 0건. 회원 차단 1건 발생 시 audit_logs 테이블에 risk_level='critical'·session_id 채워짐 (실제 운영 검증은 머지 후 라이브에서 확인 필요)
- **변경 파일**: `lib/audit.ts`

### 7.2 BUG-17-05 [LOW → ✅] 연속 로그인 실패 카운트 갱신

- **조치**: 어드민 로그인 함수와 사용자 로그인 함수 양쪽에서
  - 비밀번호 불일치 시: `login_fail_streak += 1` (기존 누적 카운트와 별개)
  - 로그인 성공 시: `login_fail_streak = 0`으로 리셋
- **기존 누적 카운트(`login_fail_count`)와의 차이**: 누적 카운트는 잠금 임계치를 다루고, 연속 카운트는 "지금 시점에서 연속 실패 N회"를 항상 정확히 보여줌
- **검증 결과**: TypeScript 신규 에러 0건. 비밀번호 3회 틀림 → DB streak=3, 정답 입력 → 0 리셋 (라이브 검증은 머지 후 확인 필요)
- **변경 파일**: `netlify/functions/admin-login.ts`, `netlify/functions/auth-login.ts`

### 7.3 BUG-17-06 [MEDIUM] 영향 파일 조사 결과 (코드 수정 보류)

먼저 마스킹 대상 컬럼을 확인. 회원 테이블에 주민번호(rrn) 컬럼은 **존재하지 않음** — 시스템이 주민번호를 보관하지 않으므로 마스킹 대상은 사실상 **전화번호(phone) 단독**.

#### 어드민 응답에 phone을 그대로 노출하는 함수: 21개

| 그룹 | 파일 | 회귀 위험 | 비고 |
|---|---|---|---|
| **회원 관리** (6) | `admin-members.ts`, `admin-member-detail.ts`, `admin-members-blacklist.ts`, `admin-pending-approvals.ts`, `admin-operators.ts`, `admin-eligibility-list.ts` | **중** | 어드민이 회원에게 직접 연락할 때 원본 필요. 정책 분기(super_admin은 원본, 일반 admin은 마스킹) 또는 화면 표시 시점 클라이언트 마스킹 권고 |
| **후원자 관리** (5) | `admin-donor-regular-list.ts`, `admin-donor-prospect-list.ts`, `admin-donation-pending-list.ts`, `admin-donation-confirm.ts`, `admin-churn-risks.ts` | **중** | 후원 처리·환불·이탈 대응 시 연락처 사용. 정책 분기 또는 모달 진입 시 원본 표출 권고 |
| **데이터 익스포트** (3) | `admin-members-export.ts`, `admin-members-contract-export.ts`, `admin-donations-export.ts` | **대** | Excel·CSV 다운로드 후 외부 활용. 마스킹 시 실제 활용 불가능 → super_admin 전용으로 권한 제한 또는 마스킹 토글 옵션 권고 |
| **효성 CMS+** (3) | `admin-hyosung.ts`, `admin-hyosung-import-contracts.ts`, `admin-hyosung-import-billings.ts` | **매우 대** | 효성과 후원자 매칭에 phone이 식별 키. **마스킹 절대 금지** (매칭 깨짐) |
| **채팅·신고·전문가** (4) | `admin-chat-rooms.ts`, `admin-experts-for-match.ts`, `admin-anonymous-reveal.ts`, `admin-support.ts` | **중** | 처리 단계에서 사용. `admin-anonymous-reveal`은 익명 신고 추적 기능이라 별도 정책 필요 |

#### 일률 마스킹의 문제점

1. 어드민 본연의 업무(회원/후원자 응대, 효성 매칭)가 깨짐
2. 익스포트 결과를 외부에서 활용 불가
3. 효성 CMS+ 매칭은 phone이 키이므로 절대 마스킹 불가

#### 권고하는 정책안 3가지 (메인 결정 필요)

- **A안 (권장)**: super_admin 권한은 원본 그대로, 일반 admin·운영자는 마스킹. 효성·익스포트 함수는 super_admin 전용으로 권한 제한
- **B안**: 백엔드는 항상 원본을 보내고, 화면에서만 클라이언트 측 마스킹(Q5의 본래 의도). 익스포트·효성은 영향 없음. 단, 응답 본문이 노출되는 환경(개발자 도구)에서 마스킹이 우회됨
- **C안**: 마스킹 적용 범위를 좁혀 "고위험 어드민 페이지(블랙리스트·삭제 화면 등)"에만 적용. 일반 회원 목록은 그대로

본 라운드에서는 **코드 수정 없이 조사만 완료**. 메인이 정책 결정 후 별도 라운드로 진행 권고.

### 7.4 후속 라운드 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `lib/audit.ts` | 세션ID·위험등급 INSERT 활성화 + `deriveSessionId(req)` 헬퍼 추가 |
| `netlify/functions/admin-login.ts` | 어드민 로그인 실패/성공 시 `login_fail_streak` 갱신·리셋 |
| `netlify/functions/auth-login.ts` | 사용자 로그인 실패/성공 시 `login_fail_streak` 갱신·리셋 |
| `docs/verify/2026-05-11-phase17.md` | 본 §7 추가 |

### 7.5 BUG 상태 요약

| BUG | 1차 라운드 | 후속 라운드 | 최종 상태 |
|---|---|---|---|
| BUG-17-01 | ✅ fix (`admin-idle-guard.js`) | — | 해결 |
| BUG-17-02 | ✅ fix (`admin.js`) | — | 해결 |
| BUG-17-03 | ✅ fix (`admin-audit-stats.ts`) | — | 해결 |
| BUG-17-04 | 미완성 영역 | ✅ fix (`lib/audit.ts`) | 해결 |
| BUG-17-05 | 미완성 영역 | ✅ fix (`admin-login.ts`·`auth-login.ts`) | 해결 |
| BUG-17-06 | 미완성 영역 | 🔍 조사 완료 (21개 파일·정책안 3종 제시) | **메인 결정 대기** |
