# Phase 22-A 매출 통합 관리 — C 검증 라운드 결과

> **작성**: C 채팅 (Opus 4.7, worktree=`tbfa-mis-C`, 브랜치 `verify/phase22a`)
> **베이스**: main @ `160e560` (B 백엔드 7 API + AI 도구 7개 머지 시점)
> **검증 일자**: 2026-05-14
> **검증 모드**: 정적 코드 정독 + 설계서 §4·§10 매핑
> **이유**: A 프론트 미머지 상태로 UI 화면 검증 불가. API + AI 도구 로직만 코드 레벨에서 검증.

---

## 0. 한눈 요약

| 항목 | 수치 |
|---|---|
| Q 총 개수 | 18 |
| PASS | 7 |
| PARTIAL (조건부 동작) | 4 |
| FAIL | 4 |
| 미검증 (UI 의존) | 3 |
| 발견 BUG | 12건 (Critical 2 · High 3 · Medium 4 · Low 3) |

**라운드 머지 가능 여부**: **❌ 불가**. Critical 2건(환불 덮어쓰기, P&L 후원환불 누락)·High 3건(권한 시드 도구명 불일치, fiscalYear 자동 계산 누락, 카테고리 필터 미구현)이 메인 fix 없이는 운영 데이터 무결성·권한 우회·핵심 산식이 깨짐.

---

## 1. Q1~Q18 검증 결과표

| Q | 시나리오 | 결과 | 근거 / BUG |
|---|---|---|---|
| Q1 | 카테고리 목록 6개 (lecture/govgrant/corp_sponsor/twork_on/twork_si/etc) 표시 | ✅ PASS | `admin-revenue-categories-list.ts` + 시드 6건 (`migrate-phase22a-revenue` 0단계). sortOrder 정렬 OK |
| Q2 | 매출 신규 등록 → draft 상태 생성 | ⚠️ PARTIAL | `status='draft'` 기본값은 OK. 단 **BUG-004**: fiscalYear 서버 자동 계산 안 함 (클라이언트 입력값 사용) |
| Q3 | 매출 목록 조회 (시드 7건 + Q2 생성건) | ✅ PASS | `admin-revenue-list.ts` fiscalYear·status 필터 + summary{totalAmount, totalRefund, netAmount}. 시드 함수 정상 |
| Q4 | 회계연도 필터 (2026 vs 2025) | ✅ PASS | `fiscalYear` 필수 파라미터, 시드에 2025/2026 혼재 (시드 6번이 2025) |
| Q5 | 상태 필터 (draft/approved/rejected/all) | ✅ PASS | `status` 옵션, `all` 시 조건 미적용 |
| Q6 | 카테고리 필터 | ❌ **FAIL** | **BUG-005**: `admin-revenue-list.ts`에 categoryId 필터 미구현. 설계서 §2.3 명시 |
| Q7 | draft 항목 수정 → 성공 | ✅ PASS | `admin-revenue-update.ts` draft만 허용, 권한 분기(super_admin OR 등록자 본인) OK |
| Q8 | approved 항목 수정 시도 → 거절 | ✅ PASS | `if (rev.status !== "draft") return 400` |
| Q9 | super_admin이 draft 승인 → status=approved 변경 | ✅ PASS | API 권한 분기 + approvedBy/approvedAt 기록 |
| Q10 | super_admin이 draft 반려 (rejectionReason 필수) → status=rejected | ✅ PASS | `if (action === "reject" && !rejectionReason) return 400` |
| Q11 | approved 항목에 환불 50만원 기록 → netAmount 감소 | ⚠️ PARTIAL | 단건 환불은 OK. **BUG-001**: 누적 환불 시 덮어쓰기 (이전 환불액 사라짐). 설계서 §2.6/§10.3 명시 위반 |
| Q12 | refundAmount > amount → 거절 | ✅ PASS | `if (Number(refundAmount) > Number(rev.amount)) return 400` |
| Q13 | P&L 응답 키 구조 (revenue.donations.net / revenue.other.byCategory[] / totalNet / monthly[]) | ⚠️ PARTIAL | 키 구조는 설계서 §2.7과 일치. 단 **BUG-002**: `donations.refund` 항상 0 |
| Q14 | 후원금(정기+일시) 합계 정확성 | ❌ **FAIL** | **BUG-002**: `donations.status='refunded'` 차감 누락. 설계서 §2.7 산식 위반 |
| Q15 | 기타매출 net = amount - refund 정확성 | ✅ PASS | `pl-summary.ts` byCategory[]에서 g - r 계산 OK |
| Q16 | "2026년 매출 목록 보여줘" → revenue_list 호출 | ⚠️ PARTIAL | 도구 정의·핸들러 OK. 단 **BUG-009**: `selectRelevantTools` `finance` 그룹에 7개 신규 도구 누락 → 안전망(null)으로 전체 도구 로딩되어 동작은 함 |
| Q17 | "강연 매출 100만원 등록" → revenue_create dry-run → 승인 → 실행 | ⚠️ PARTIAL | dry-run + INSERT 흐름은 OK. 단 **BUG-011**: AI 도구가 `categoryCode` 대신 `categoryId` 요구 (설계서 §10.2 위반). AI가 카테고리 ID 매핑 부담 |
| Q18 | admin 권한으로 revenue_approve → super_admin 전용 거절 | ❌ **FAIL** | **BUG-003**: `ai_tool_permissions` 시드 도구명(`other_revenue_approve`)과 실제 도구명(`revenue_approve`)이 다름 → 권한 entry 부재 → admin도 호출 통과 (권한 우회) |

---

## 2. 발견 BUG 12건 (심각도순)

> 각 BUG는 `docs/issues/2026-05-14-phase22a-{슬러그}.md`에 별도 보고.

### Critical (운영 데이터 무결성)

| ID | 슬러그 | 위치 | 영향 |
|---|---|---|---|
| **BUG-001** | refund-overwrite | `admin-revenue-refund.ts:67`, `ai-agent-tools.ts:3233` | 환불액이 덮어쓰기. 2차 환불 등록 시 1차 환불액 사라짐 → 손익 산식 오차 누적 |
| **BUG-002** | pl-donation-refund-missing | `admin-finance-pl-summary.ts:122`, `ai-agent-tools.ts:3278` | 후원 환불(donations.status='refunded') 차감 누락. donationRefund 항상 0 → 후원 net 과대 산정 → 순이익 과대 |

### High (권한 우회·산식 불일치)

| ID | 슬러그 | 위치 | 영향 |
|---|---|---|---|
| **BUG-003** | ai-tool-permissions-seed-mismatch | 삭제된 `migrate-phase22a-revenue.ts` Step 4 시드 vs `ai-agent-tools.ts:485-537` | 시드는 `other_revenue_*` 이름이지만 실제 도구는 `revenue_*` → super_admin 권한 entry 없음 → admin이 AI 도구로 매출 승인·환불 가능 (운영 권한 사고) |
| **BUG-004** | fiscal-year-auto-missing | `admin-revenue-create.ts:23-26·50`, `ai-agent-tools.ts:3082·3102` | fiscalYear 서버 자동 계산 누락. 클라이언트가 recognizedAt=2026-05-14 + fiscalYear=2025 보내면 그대로 저장 → 회계연도 필터·집계 오류 |
| **BUG-005** | revenue-list-filter-incomplete | `admin-revenue-list.ts:11-21` | 설계서 §2.3 명시한 categoryId·payerName(LIKE)·기간(from·to) 필터 모두 미구현. Q6 검증 자체 불가, 프론트 화면에서 카테고리 드롭다운 동작 못 함 |

### Medium (사양 일치 + UX)

| ID | 슬러그 | 위치 | 영향 |
|---|---|---|---|
| **BUG-006** | refund-role-conflict | `admin-revenue-refund.ts:17`, `ai-agent-tools.ts:527` | API는 super_admin 요구하지만 설계서 §10.1·시드 의도는 admin. 또한 AI 도구는 시드 entry 없어 admin도 통과(BUG-003) → API/AI 권한 모순 |
| **BUG-009** | select-relevant-tools-finance-miss | `admin-ai-agent.ts:293-294` | `finance` 그룹에 Phase 22-A 도구 7개·키워드(매출·수입·손익·순이익·강연·정부·기업·협찬·함께워크) 모두 누락. 안전망(null→전체 도구)으로 동작은 하나 효율·신뢰성 저하. 설계서 §10.6 위반 |
| **BUG-010** | tool-description-enum-missing | `ai-agent-tools.ts:486·489·500·527 등` | 도구 description에 카테고리 6코드(`lecture\|govgrant\|corp_sponsor\|twork_on\|twork_si\|etc`) enum 미명시. 설계서 §10.7·표준 v1.4 §18.13 위반 |
| **BUG-011** | tool-category-id-vs-code | `ai-agent-tools.ts:493` | `revenue_create` 도구가 `categoryCode` 대신 `categoryId`(INTEGER) 요구. AI가 ID 매핑 필요 → 자연어 처리 비효율. 설계서 §10.2 위반 |

### Low (메서드·키명 불일치)

| ID | 슬러그 | 위치 | 영향 |
|---|---|---|---|
| **BUG-007** | revenue-update-method | `admin-revenue-update.ts:9` | 설계서 PATCH인데 PUT 구현. A 프론트가 설계서 따른 경우 405 |
| **BUG-008** | revenue-approve-key-mismatch | `admin-revenue-approve.ts:28` | 설계서 `revenueId/reason` vs 구현 `id/rejectionReason`. AI 도구는 `id` 사용으로 호환되나 A 프론트가 설계서 따른 경우 400 |
| **BUG-012** | categories-list-all-option | `admin-revenue-categories-list.ts:14-17` | `?all=1` 옵션 미구현, isActive 필터 자체 없음 → 항상 전체 반환. 시드 6개 모두 active이므로 실용 영향 없음. 설계서 §2.1 사양 차이만 |

---

## 3. 미검증 항목 (UI 의존)

A 프론트(`public/admin-other-revenues.html`·`public/js/admin-other-revenues.js`)가 main에 미머지 상태라 다음 항목은 코드 검증 불가:

- 사이드바 신설 "💰 재정 관리" 그룹 5개 메뉴 진입 (설계서 §3.1)
- "후원 외 매출 관리" 화면 [+ 매출 추가] 모달·필터·요약 표시 (§3.2)
- 수입현황 통합 KPI 6개·월별 추이 차트 (§3.3)
- 재무보고서 손익계산서 탭 (§3.4)
- Cmd+K 검색·즐겨찾기 통합

→ A 머지 후 별도 라운드 검증 권장.

---

## 4. 권한 분기 검증 (Swain 요구)

설계서·트리거 명시 권한:

| 도구 / API | 설계서 명시 권한 | API 실제 | AI 도구 권한 시드 | AI 도구 분기 | 평가 |
|---|---|---|---|---|---|
| `revenue_categories_list` | admin | requireAdmin | NULL 시드 | 통과 | ✅ |
| `revenue_list` | admin | requireAdmin | **시드 누락** (other_revenues_list만 있음) | 통과 (시드 없음=open) | ⚠️ admin OK이나 시드 일관성 깨짐 |
| `revenue_create` | admin | requireAdmin | **시드 누락** | 통과 | ⚠️ 동일 |
| `revenue_update` | (신설) admin + 등록자/super | API에서 분기 OK | **시드 자체 없음** | 통과 | ⚠️ AI는 분기 못 함 |
| `revenue_approve` | **super_admin** | super_admin 분기 OK | **시드 누락** (other_revenue_approve만 있음) | **통과 ❌** | **BUG-003** super_admin 우회 |
| `revenue_refund` | admin (설계서 §10.1) | **super_admin** (API 분기) | **시드 누락** | 통과 | **BUG-006** API/설계서 모순 |
| `pl_summary` | admin | requireAdmin | NULL 시드 | 통과 | ✅ |

**결론**: Q18 의 핵심 가설(admin이 AI 도구로 매출 승인 시도 → 거절)이 **불통**. 시드 도구명을 `other_revenue_*` → `revenue_*`로 정정해야 권한 분기가 작동.

---

## 5. 회귀 영역 점검

설계서 §8.7·트리거 push 전 체크 항목:

| 영역 | 결과 | 비고 |
|---|---|---|
| `lib/ai-agent-tools.ts` 기존 80여 도구 동작 | ✅ 무영향 | 신규 7개는 파일 끝 append-only |
| `lib/ai-agent-config.ts` FALLBACK_SYSTEM_PROMPT | ✅ 매핑 표 7줄 추가 | 기존 매핑 영향 없음 |
| `db/schema.ts` append-only | ✅ 무영향 | `/* === Phase 22-A === */` 섹션 헤더 명시 |
| `admin-finance-pl-summary.ts` 신규 — donations·other_revenues 기존 데이터 영향 | ✅ 무영향 | SELECT만 |
| **selectRelevantTools `finance` 그룹** | ❌ 갱신 누락 (BUG-009) | 기존 4개 그대로, 신규 7개 미반영 |

---

## 6. 메인 권장 fix 우선순위

```
[즉시 — 머지 차단 해소]
1. BUG-001: refund-overwrite — refundAmount += 누적 (트랜잭션) → API + AI 도구 동일
2. BUG-002: pl-donation-refund-missing — donations.status='refunded' 집계 추가 (월별 분해도)
3. BUG-003: ai-tool-permissions seed 도구명 정정 — `other_revenue_*` → `revenue_*` 7건 갱신 (revenue_update 시드 신규 추가 포함)
4. BUG-004: fiscalYear 서버 자동 계산 — recognizedAt.slice(0,4)로 강제
5. BUG-005: revenue-list categoryId 필터 추가 (payerName·기간은 후순위 가능)

[1주 내]
6. BUG-006: refund 권한 의도 확정 — 설계서 §10.1(admin) 또는 API(super_admin) 중 하나로 통일
7. BUG-009: selectRelevantTools `finance` 그룹에 7개 신규 도구 + 키워드(매출·수입·손익 등) 추가
8. BUG-010: 도구 description에 카테고리 6코드 enum 명시
9. BUG-011: tool_revenueCreate가 categoryCode(STRING) 받도록 변경 + 핸들러에서 categories 조회 → id 매핑

[A 머지 시점 정정]
10. BUG-007: PATCH 메서드 허용 (또는 설계서를 PUT으로 갱신)
11. BUG-008: 요청 키명 통일 — 설계서를 코드(id/rejectionReason)에 맞추거나 코드를 설계서(revenueId/reason)에 맞춤
12. BUG-012: categories-list ?all=1 옵션 추가
```

---

## 7. 검증 데이터 영향

C 본 라운드는 코드 정독·라이브 호출 없음 → **DB INSERT 0건**. `migrate-phase22a-c-seed?run=1`은 Swain 직접 호출 시점에 7건 INSERT (description 끝 `[verify]` 마커).

검증 완료 후 시드 정리: `migrate-phase22a-c-seed?cleanup=1`.

---

**보고 끝**. fix 후 재검증 시 `verify/phase22a-r2` 브랜치 권장.
