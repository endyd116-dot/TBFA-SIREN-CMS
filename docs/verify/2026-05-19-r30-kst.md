# R30 KST 표시 영역 통일 라이브 검증 보고서

> 검증자: C 채팅 (Opus 4.7)
> 시각: 2026-05-19 (KST)
> 브랜치: verify/r30-kst (main 기반)
> 대상 커밋: **main @ 5b5b1ff** (R30-A 61f45e1 + R30-B 1e0c594 머지 완료)
> 라이브 URL: https://tbfa.co.kr

---

## §1 검증 환경

| 항목 | 값 |
|---|---|
| 커밋 (검증 시점 main) | e88e33a (= 5b5b1ff + state docs) |
| 변경 통계 | 82 files / +431 / -140 |
| 영역 | 프론트 14페이지 (R30-A) + 어드민 33페이지 + 이메일·alert·lib/datetime (R30-B) |
| 검증 도구 | grep + 코드 정독 + git diff + node hand-eval |

---

## §2 결과 표 — Q1~Q7

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| Q1 | 공통 헬퍼 안착 | ✅ PASS | `public/js/lib-kst.js` 존재 + 6함수(`fmtKST`·`fmtKSTDate`·`fmtKSTDateTime`·`fmtKSTLong`·`fmtKSTTime`·`fmtKSTRelative`) + `window.*` 노출. 14페이지(워크스페이스 8 + 사용자 6: incident·incidents·legal-support·my-reports·mypage·report-harassment) + 어드민 33페이지 = **lib-kst 로드 47페이지 정확 일치**. node check 통과 |
| Q2 | lib/datetime.ts 백엔드 헬퍼 | ✅ PASS | 4함수(`fmtKSTForEmail`·`fmtKSTSimple`·`fmtKSTDate`·`toKstIso`) 모두 존재. `toDate(input)` 내부 헬퍼가 null/undefined/""/NaN 안전 처리 → 모든 export 함수 입력 검증 |
| Q3 | 사용자/워크스페이스 KST | ✅ PASS | `workspace-kanban.js:1420·1431`·`workspace-notifications.js:47`·`mypage-eligibility.js:29`·`mypage-expert-match.js:46`에서 `window.fmtKST*` 우선 + fallback도 `timeZone: 'Asia/Seoul'` 옵션 명시. workspace-milestones의 `toLocaleString()` 잔재는 모두 **숫자 포맷**(`Number(...).toLocaleString()`)이지 시간 아님 |
| Q4 | 어드민 KST | ✅ PASS | `Asia/Seoul` 옵션 명시 패턴 **18건** (admin-expert·notification-defaults·notification-logs·report·security-audit·send-job-create·templates 등). B 적응 보고 정합 — `window.fmtKST*` 호출 0건, 옵션 명시 패턴 일관 채택으로 머지 의존성 0. 시간 표시용 옵션 없는 `toLocaleString()` 잔재 0 (숫자 포맷 제외) |
| Q5 | 이메일·AI·알림 body KST | ✅ PASS | `lib/email.ts:2` import + 7군데(`donationDate`·`createdAt`·`withdrawnAt`·`chargedAt`·`nextChargeAt`·`approvedAt`·`rejectedAt`)에서 `fmtKSTSimple`/`fmtKSTDate` 사용. `lib/ai-task.ts:215` 활동 요약 `toLocaleDateString("ko-KR", {timeZone:"Asia/Seoul"})` 정정. `admin-task-due-changes.ts:230`·`admin-workspace-events.ts:336·506`·`admin-workspace-tasks.ts:557` 알림 body 모두 옵션 명시 |
| Q6 | 회귀 점검 | ✅ PASS | ① schema·cron-*·toss-billing·hyosung-parser 변경 0 (grep) ② `toISOString()` 라인 변경 0 (UTC 통신용 보존) ③ 결제 PG·DB 저장 영역 영향 0 |
| Q7 | 브라우저 시간대 무관성 | ✅ PASS | node hand-eval — `TZ=America/New_York` 환경에서도 `toLocaleString('ko-KR', {timeZone:'Asia/Seoul'})` 결과 동일 (`2026. 5. 19. PM 7:00:00`). 옵션 명시 패턴이 OS·브라우저 시간대 의존성 제거 |

---

## §3 BUG 발견

**없음.** Q1~Q7 모두 PASS.

---

## §4 잔재 grep 결과 (정보 — 후속 정리 권장 0건)

### 옵션 명시 없는 `toLocaleString()` 라이브 호출처 (이번 라운드 범위 외)

| 위치 | 형태 | 분류 |
|---|---|---|
| `public/js/workspace-milestones.js:289·290·419·541~569` | `Number(x).toLocaleString()` | 숫자 포맷 — 정상 |
| `public/js/admin-activity-report.js:233~245` | `(x \|\| 0).toLocaleString()` | 숫자 포맷 — 정상 |
| `public/js/admin-anon-audit.js:74~77` | `(x \|\| 0).toLocaleString()` | 숫자 포맷 — 정상 |
| `public/js/admin-approvals.js:110·148` | `(n \|\| 0).toLocaleString()` | 숫자 포맷 — 정상 |

**전체 옵션 없는 `toLocaleString()` 잔재는 모두 숫자 포맷**. 시간 표시용 잔재 0건. R30 범위 완료.

### lib/ai-task.ts:228 ISO 형식 잔존 (BUG 아님)

```typescript
- 시작 → 완료: ${task.createdAt} → ${task.completedAt || new Date().toISOString()}
```

→ AI 프롬프트 컨텍스트로만 사용 (사용자 표시 아님). ISO 형식이 AI 모델에 더 명확하므로 의도적 유지. R30 정책 위반 아님.

---

## §5 운영 권장 (검증과 무관 — 이전 라운드 후속)

| # | 액션 | 상태 |
|---|---|---|
| OP-1 | 2026 Q2 분기 등록 — admin-milestone-settings 분기관리 탭 | 🟡 여전히 미수행 (P1·P2 거쳐서도 후속 안 됨) |
| OP-4 | 카카오 주소 검색 placeholder 안내 ("예: 서울특별시 강남구 강남대로") | 🟢 1줄 patch 가능 |

---

## §6 검증 통계

- **PASS**: 7 / 7 (100%)
- **FAIL**: 0
- **BUG**: 0
- **lib-kst 로드 페이지**: 47개 (A 14 + B 33) 정확 일치
- **시간 표시 옵션 잔재**: 0건 (모든 잔재가 숫자 포맷)
