# R45-MASTER — 운영 시작(06.01) 전 최종 점검 수합·우선순위

> 수합: 메인(Opus) / 2026-05-29 / 베이스 9040dfb
> 입력: R45-report-S(슈퍼어드민·메인)·-A(어드민)·-B(운영자)·-C(사용자) + 메인 직접 재검증
> 방식: 4영역 읽기전용 감사 → 본 마스터로 중복제거·클러스터·우선순위 → 한 번에 fix → tsc → **06.01 전 단일 배포**

## 0. 4영역 집계

| 영역 | 역할 | 건수 | P0 | P1 | 검증 |
|---|---|---|---|---|---|
| S(메인) | 슈퍼어드민 | 38 | 1 | 8 | 7영역+P0/P1 반증 |
| A | 어드민 | ~87 | 0 | 32 | 38에이전트+직접 재검증 |
| B | 운영자 | 78 | 5 | 19 | 16에이전트+적대검증 |
| C | 사용자 | 66 | (1)* | 17 | 2웨이브+메인 직접 |
| **합계(원시)** | | **~269** | **7** | **76** | (클러스터 dedup 후 고유 이슈는 더 적음) |

*C는 P0를 0으로 분류했으나 US-011은 2차 적대검증 P0 판정 → 실질 P0.

---

## 1. 🔴 운영 시작(06.01) 전 반드시 — 클러스터별 통합

### CLUSTER-1 〔P0〕 인가 출처 혼용 — 권한 상승 (★3영역 독립 확정·최우선)
**메인·A·B가 독립적으로 같은 뿌리를 짚음.** `auth-admin-elevate.ts:51`이 `type==='admin'`이면 DB 역할 무관 **super_admin JWT를 발급** → JWT 역할(`ctx.admin.role`)을 신뢰하는 게이트가 무력화. (운영자는 DB상 type=admin/role=operator라 통과; 정상 로그인·세션연장은 DB 역할이라 안전. JWT는 서명되어 위조는 불가 — A 정정.)

- **종착점(JWT 역할 신뢰 게이트, DB 역할로 교체 필요)**:
  - `admin-role-permissions.ts:28` ★권한정책 매트릭스 토글(전 어드민 접근의 마스터) = **SU-009·OP-002·AD-001 — 최고 폭발력**
  - `admin-revenue-approve.ts:17`(수입 승인/반려) = SU-019·OP-004
  - `admin-expense-category-update.ts:17`·`admin-revenue-update.ts:54`·`admin-expense-update.ts:55`(타인 전표 수정·계정과목) = OP-005
  - `admin-donation-confirm.ts:310`(후원 통과 canAccess) = OP-006
  - AI 도구 권한 `admin-ai-agent.ts:659`·`-stream.ts:70`+`checkToolAllowed` = SU-003·OP-003 (변경도구 대다수 `ensureRole` DB 백스톱 없음)
- **단일 수정점 + 이중 방어**: ① `auth-admin-elevate`를 `role: members.role`(DB) 발급으로 (admin-login과 통일) → 위 다수 동시 차단. ② 그래도 위 게이트는 `requireRole(ctx.member,…)`/`canAccess(ctx.member.role,…)`(DB)로 직접 교체(이중 방어).
- **⚠️ 배포 순서 의존(CLUSTER-4 선행 필수)**: 지금은 elevate 버그로 canAccess가 항상 true(super_admin). elevate를 DB 역할로 고치는 순간, `role_permissions`에 operator 허용이 시드 안 된 기능들이 **운영자에게서 갑자기 막힌다**(회귀). → **CLUSTER-4(featureKey 시드)를 elevate fix와 같은 배포에, 시드 먼저** 반영.

### CLUSTER-2 〔P0/P1〕 익명성·민감 PII 노출 (신고 플랫폼 신뢰 핵심)
- **OP-007 〔P0〕** 익명 SIREN 신고 **목록·상세에 제보자 실명 마스킹 누락**(사건·괴롭힘 목록 `memberName`만 null·`reporterName` 잔존 / 상세 `...r` 무차별 spread). 3종 동일 패치. ※C는 "신고 본문 익명성 정상"이라 했는데 이는 **사용자 화면** 기준이고, OP-007은 **운영자 화면**의 reporter* 필드 노출 — 층위 다름·둘 다 사실.
- **US-039 〔P1〕** 게시판 글 익명 전환 시 작성자 실명이 공개 목록·상세 노출.
- **AD-003·OP-008 〔P0/P1〕** 익명→실명 전환(reveal)을 **운영자도 단독 실행** + super_admin 통지·게이트 부재 + 본인 미통지(AD-079). 매뉴얼은 "슈퍼 전용"이라 안내(불일치). → reveal·reveal-logs에 `requireRole(super_admin)`/canAccess + super_admin 즉시 통지.
- **AD-042** 익명 제보 상세·목록이 `memberId` 그대로 반환(우회 식별). **US-018** 내 신고 상세 과다 컬럼 반환(내부 운영필드). **SU-025** reveal이 통합 감사로그 미연동.
- **AD-077·AD-078·AD-079** reveal 단계(요청→검토→승인) 부재 + 본인 실명전환 요청 경로 부재 + 본인 미통지.

### CLUSTER-3 〔P0/P1〕 무인증·IDOR
- **US-011 〔P0〕** 무인증 시뮬결제 `/api/donate` 잔존 — 누구나 가짜 '완료' 후원 무한 생성 + 임의 이메일로 협회 명의 메일 + 통계 오염. **★메인 직접 확정: 프론트 미참조 고아(R40 전 잔재) → 파일 삭제가 fix.**
- **OP-009·010 〔P1〕** AI 대화 목록/상세 IDOR — 운영자가 타 관리자 AI 대화 전문·도구 입출력(회원·재정 PII) 열람.
- **OP-033·034·035 〔P1/P2〕** 워크스페이스 하위업무·작업보고서·첨부 목록 조회에 소유 검증 없음(전사 열람·작성자 PII).
- **OP-042 〔P1〕** AI 재생성이 작업 소유권 미검증(타인 작업 덮어쓰기·AI 비용).
- **SU-018 〔P1〕** 감사 로그 목록·통계가 슈퍼 전용 아님(`admin-audit-list`·`-stats`) — 운영자가 전 관리자 활동·IP·PII 열람. (OP-014와 동일.)
- **US-012 〔P2〕** 카드만료 등록 API 무인증·소유자 미검증(만료월 변조). **AD-021** 딥릴리프 검토 승인이 배정자 미검증.

### CLUSTER-4 〔P1〕 featureKey 시드 부재 (CLUSTER-1 선행 필수)
- **AD-002·SU-005·OP-052** `role_permissions` INSERT가 코드·마이그 어디에도 없음 → 미등록 키는 "admin 허용·operator 차단" 기본값 고정. 운영자 위임 불가·매트릭스 빈 화면·발간 export 운영자 안내와 불일치(AD-035). 근태/성과 AI 키는 FEATURE_REGISTRY 미등록이라 토글 자체 불가(OP-052).
- **fix**: 49개 featureKey 시드 마이그(라벨·카테고리·기본 admin/operator 허용값·멱등 INSERT) + 매트릭스 UI '미등록 키 자동 노출'. **elevate fix와 같은 배포에, 시드 먼저.**

### CLUSTER-5 〔P1〕 알림·워크플로우 단절 (갭 — 사용자 후속 암전)
- **신고 처리 무통지**: AD-014(답변 없이 상태만 바꾸면 통지 0)·AD-015(단계이력·타임라인 전용 API가 고아 — 진행이력 영구 미기록)·US-021. → 상세 상태변경을 `admin-report-status-update` 경유로 통일 + 사용자 타임라인 연결.
- **유족지원 상태 무통지**: US-022·AD-018(직접배정이 원본 신청서 미반영·'미배정' 유지)·AD-019(배정·종료 양측 알림 0). 자격변경은 통지 정상(대비).
- **전문가 배정 알림**: OP-060·US-051·AD-019.
- **상담 알림**: OP-057(새 상담/메시지 운영자 알림 0)·OP-055(1:1 상담 CMS 미통합)·OP-061(자동 종료 부재).
- **마감일 변경**: OP-043·044·049 — 요청/승인 UI 전체 부재(백엔드만 살아있는 죽은 기능).
- **효성 자동이체 단절**: AD-009(대량등록 추출 항상 0건·상태값 불일치)·AD-010(미납 알림·재시도 부재)·AD-011(미매칭 수동연결 화면 부재).

### CLUSTER-6 〔P1〕 데이터 파기/고아 (개인정보 신뢰)
- **SU-024·AD-020** 순직 사건 통째 삭제 시 RAG 청크·R2 원본(진료·진술)·외부자료 역참조 고아 잔존 — 화면은 "모두 삭제" 약속. doc-delete 정리 로직 재사용.
- **AD-030** 추모 선생님 삭제 시 헌화·방명록·편지 고아('함께 정리' 거짓 안내).
- **OP-075·US-028 계열** 콘텐츠/추모글 하드삭제·셀프삭제 부재.

### CLUSTER-9 〔P1〕 죽은 기능·정합 오류 (즉시 동작 불능)
- **US-001** 봉사자·전문가 가입 100% 거절(모달 키 volunteer/expert ↔ 서버 키 불일치). **전문가 가입이 상담매칭에 필수면 P0** — Swain 확인.
- **US-007** 마이페이지 회원정보 저장 항상 405(죽은 기능). **US-057** 뱃지 화면 영구 500.
- **AD-006** 자격 강제변경 영문 4종 ↔ 서버 한글 enum 불일치(항상 거절). **AD-016/073** 신고 담당자 드롭다운 항상 빈·'미할당'(응답 키 불일치).
- **US-059/060** 리워드 교환 재고 무차감 + 트랜잭션 부재 음수잔액(포인트 이중차감).
- **AD-069·SU-016** 예산 편성 API가 폐기된 budgets 테이블 쓰기 + AI 비서가 폐기 테이블 읽기(화면과 단절).
- **US-058 〔P1·컴플라이언스〕** 알림 수신거부 키(underscore) ↔ 발송 디스패처 키(점표기) 불일치로 **끈 알림도 계속 발송**(수신동의 법적 리스크).

---

## 2. 🟡 P2/P3 묶음 (단일 배포에 함께·영역 보고서 ID 참조)

- **감사로그 강화**: SU-023(권한변경 미기록)·SU-026(위험등급 매핑 불일치)·OP-018(수입 승인 감사 0)·OP-070(댓글중재 감사 0). lib/audit.ts RISK_LEVEL_MAP을 실제 액션명으로 정정·1곳 정본화.
- **cron 멱등/등록**: SU-035(카카오 상태추적 cron netlify.toml 미등록)·SU-036(만근보너스 재실행 중복적립)·SU-037~040(retry/dispatcher/스케줄러 선점 lock·외부수집 실패 시 last_cron_at)·SU-041/042(상태 강등·요일 미파싱).
- **재정 비대칭**: SU-017(환불 비원자적 race)·SU-019/OP-004(수입승인 JWT)·AD-012/SU-020(반려 재제출 경로)·AD-067/068(반려 사유 필수/표시 불일치)·AD-013(PDF 귀속일 불일치)·AD-070/071(효성 import 매칭·이중계상)·SU-022(전표 승인자 형식).
- **AI 비용/설정 권한**: SU-027(월한도 fail-open)·SU-028/OP-015/016(설정·비용 화면 운영자 통과)·OP-047(재생성 빈도제한)·OP-053(대화 삭제/정리 부재)·OP-054(RAG 격리 fail-open 구조).
- **워크스페이스**: OP-036(공유 만료 필터)·OP-037(미초대 RSVP)·OP-038(offset 페이지네이션)·OP-039/040.
- **딥릴리프 검수 게이트**: AD-021/022/023(검수자 배정·자기검수·미검수 내보내기)·AD-024/025(출처 사건 선택·목차 재제안 본문 삭제)·AD-026(외부수집 설정 UI)·AD-080/081/082/083/084(발간 실패 더미·마스킹·서면 권한·외부검토 탭 무연동·외부삭제 게이트).
- **콘텐츠/게시판**: OP-068(게시글 영구삭제)·OP-069(예약 발행)·OP-071(팝업·큐레이션 감사·URL 검증)·OP-072(신고 대상 본문)·OP-073(캠페인 후원자 집계 불일치)·OP-075~078·US-044/046/049.
- **사용자 여정 잔여**: US-002(가입 인증메일 미발송)·US-008/009(탈퇴 복구·재가입 문구 모순)·US-013(비회원 영수증)·US-014(정기 첫결제 실패 후속)·US-018~027(신고/지원 상세·첨부·차단가드)·US-030(추모 신고수 조작)·US-050(상담 어드민메모 노출).

---

## 3. 🗄️ 마이그레이션 필요 (Swain 호출·설계 합의 후)
- **featureKey 시드** (CLUSTER-4·★elevate fix 선행 필수) — 49키 라벨/카테고리/기본 admin·operator 허용 멱등 INSERT.
- US-005 `members.phone` 부분 UNIQUE(withdrawn 제외) — 동시 가입 중복 방지.
- US-030 추모 신고 로그 UNIQUE — 신고수 조작 방지.
- US-042 워크스페이스 parentId self-FK / 신고 `rejected_reason` 컬럼(AD-048) / 가입반려 전용 status(OP-045) / expense·revenue rejected 재제출(AD-012) / vouchers.approved_by integer 통일(SU-022).

## 4. 🧭 정책 결정 필요 (Swain) — 운영자 직무 경계
> "운영자(operator)에게 어디까지 열 것인가"는 코드가 아니라 정책. CLUSTER-1 fix 후 의도값을 featureKey로 시드.
1. **근태 중간관리자**(OP-019) — 운영자가 휴가·정정 결재·현황 조회 가능해야 하나? (현재 super_admin 1인 병목)
2. **재무 열람/입력**(OP-011~016, SU-015, AD-029·036·037) — 빌링키(평문 토큰)·전사 재무·은행·효성·대량발송을 운영자에게? (빌링키 평문은 super 한정 권장)
3. **회원 PII**(OP-013) — 전 회원 검색·연락처·엑셀 export를 운영자에게?
4. **민감 상담**(OP-056·058·067) — 법률·심리 1:1 상담 열람·채팅 블랙을 운영자에게?
5. **댓글/게시판 중재**(OP-070) — 운영자가 신고 댓글 숨김·삭제?
6. **전문가 가입**(US-001) — 운영 시작에 전문가(변호사·심리) 가입이 필수면 P0.
7. **비회원 후원/영수증**(US-013) — 비회원 후원 허용 + 영수증 경로 vs 가입 강제.
8. **익명 reveal 단계**(AD-077·OP-008) — super 전용 + 승인 단계 + 본인 통지 도입 여부.

## 5. ⚙️ 환경변수·운영 체크 (코드 무관)
- `SITE_URL=https://tbfa.co.kr`(US-010·미설정 시 메일 링크가 Netlify 도메인) / `INTERNAL_TRIGGER_SECRET`(background fail-closed) / KICC·솔라피·`SIREN_SSO_SECRET`·`SIAX_SSO_SECRET`(SU-048 .env.example 누락) / 카카오 cron toml 등록(SU-035).

## 6. 📋 권장 fix 순서 (단일 배포·06.01 전)
1. **CLUSTER-4 시드 마이그 작성** → Swain 호출(운영자 허용 의도값 포함) — **elevate fix보다 먼저 적용**(회귀 방지).
2. **CLUSTER-1**: elevate DB 역할 발급 + JWT 역할 게이트 7곳 DB 교체(메인 직접 — `ctx.admin.role` grep 전수 1회 정정).
3. **CLUSTER-2/3**: 익명 reporter 마스킹 3종 + reveal 게이트·통지 + US-011 파일 삭제 + IDOR 소유검증(OP-009/010/033/034/035/042, US-012, SU-018 super 게이트).
4. **CLUSTER-5/6/9**: 알림 단절·효성·마감일 UI·데이터 파기·죽은 기능(US-001/007/057·AD-006/016·US-058·US-059/060) — 영역별 분배 가능(A=어드민·B=운영자/워크스페이스·C 검증).
5. P2/P3 묶음(§2) 동봉 → `npx tsc --noEmit`(★머지본 재검증·feedback_audit_round) → **1회 push·배포**.

## 7. ✅ 안심 (전 영역 정상 확인 — 회귀 점검 기준선)
- 핵심 super_admin 게이트 대다수 DB 역할로 정확 차단(운영자 승급/강등·근태 17개·지출/예산 승인·환불·감사 CSV·후원정책·등급·순직 인정요건/삭제).
- ★익명 신고 **사용자 화면** 익명성·핵심 IDOR(본인 데이터만)·첨부 IDOR(R41 P0 재발 없음).
- 결제 코어(서버 금액대조·KICC 멱등·이중청구 차단·해지 연동)·인증 코어(브루트포스/타이밍/토큰 1회용)·발송 멱등(중복발송 방지)·카카오 검수 상태 관리·KICC 자동청구 이중방지·background SECRET fail-closed.

---

**다음**: Swain이 §4 정책 8건 결정 → CLUSTER-4 시드 + CLUSTER-1~3 메인 직접 fix → 영역 분배(§6.4) → tsc → 단일 배포. 본 마스터가 fix 라운드 단일 출처.
