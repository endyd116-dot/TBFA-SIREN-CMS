# 성과관리 3차 갭 분석 (2026-05-19)

조사 대상: Phase 24 + Phase 28 명세서 (전수) — R32-P0 + base_salary 마이그 + FIX-1·FIX-2 후 잔여 갭
조사자: B 채팅 Opus 4.7
기준 시점: main @ fb13db7 (R32-P0-B 머지 + base_salary 활성화 직후) — 분석은 origin/main HEAD bca7673(R32-P0-FIX-1·FIX-2 라우팅 패치)까지 반영

---

## §1 종합 진단

R32-P0 4건(C1~C4) + 라우팅 FIX-1·FIX-2 2건 머지 후 round2 H1·H2·H3·H4·M1 + base_salary 모두 해결. 다만 **R32-P0 verify §5.3 "후속" 권장 사항 — segment 라우팅 패턴 전반 점검**이 미적용 → **동일 패턴의 6개 함수에 라우팅 실패 BUG 잠재 확정**. 추가로 **EVENT_RANGE 매출의 결산 인센티브 0원 BUG**와 **함수 이중화(API 4쌍 중복)** 2건 신규 식별. round2 미처리 7건(H5·M2~M7)은 모두 그대로 잔존.

| 분류 | 신규 (이번 라운드) | round2 잔존 | 합계 |
|---|---|---|---|
| H 우선순위 | 3건 (라우팅·계산·이중화) | 1건 (H5 EVENT_RANGE 단위) | 4건 |
| M 우선순위 | 4건 | 6건 (M2·M3·M4·M5·M6·M7) | 10건 |
| 부분 구현 🟡 | 1건 (역할 배정 이중화) | 4건 | 5건 |
| ✅ 완전 구현 | (R32-P0 fix 후 갱신 — §7) | | 24건+ |

**전체 평가**: 핵심 로직(계산·결산·알림·AI·CSV·HOLD·breakdown·N/2 선택·D-N)은 완성도 높음. 그러나 **라우팅 패턴 1군데(R32-P0-FIX-1·FIX-2 와일드카드)만 fix되고 동일 패턴 6개 함수는 미적용**으로 운영자가 결산 승인·분기 활성화·역할 배정·EVENT_RANGE 금액 확정 등 핵심 어드민 액션 모두 깨짐 잠재. EVENT_RANGE는 매출연동 시드(sm-006·pm-005)에 한해 결산 시 인센티브가 0원으로 잘못 계산 (명세 Phase 28 §0 "amount 컬럼 재활용" 의도 미반영).

**머지 후 권장 라운드(R33-P0 또는 R32-FIX-3)**: H1(라우팅 5건 와일드카드 일괄) + H2(applyFormula·calcIncentive EVENT_RANGE 추가) — 코드 변경 < 30줄.

---

## §2 신규 H 우선순위 (표)

| ID | 명세 §위치 | 현재 상태 | 누락·결함 | 권장 해결 |
|---|---|---|---|---|
| **H1** | Phase 24 §2 (`PATCH/POST /:id` segment 라우팅) | R32-P0-FIX-1·FIX-2가 `milestone-definitions*`·`milestone-nonrevenue*` 2개만 와일드카드 적용. 동일 패턴의 다른 함수 6개는 path config 정확 매칭(`/api/admin-milestone-revenue` 등)만 유지 | **라이브 운영 깨짐 6건**(클라이언트가 segment URL로 호출하나 Netlify Functions v2 라우팅 안 됨 → 404 HTML 반환):<br>① `admin-milestone-revenue.ts` — `POST /:id/verify·reject` + `PUT /:id`(EVENT_RANGE 금액 확정). [workspace-milestones.js:710·724·740] 호출 → **매출 검증·반려·EVENT_RANGE 확정 모두 깨짐**.<br>② `admin-milestone-nonrevenue.ts` — `POST /:id/review·verify·reject` + `PATCH /:id/event-range`. → **비매출 1차 검토·승인·반려 깨짐**.<br>③ `admin-milestone-settlement.ts` — `POST /:id/approve·reject·paid·hold·resume`. [admin-milestones.js:467·476·484·494·503] 호출 → **결산 승인·반려·지급완료·보류·복귀 모두 깨짐**.<br>④ `milestone-quarters.ts` — `PATCH /:id`. [admin-milestones.js:331·339] activateQuarter·endQuarter → **분기 활성화·종료 깨짐**.<br>⑤ `milestone-members.ts` — `PATCH /:id/role`. [admin-milestones.js:563] saveRole → **운영자 milestoneRole 배정 깨짐**.<br>⑥ `milestone-settlement.ts` — `POST /calculate·/submit`. [workspace-milestones.js:1073·1107] → **결산 자동계산·제출 깨짐**(운영자가 분기 결산 작업 불가).<br>※ R32-P0 verify §5.3 "FIX-2 후속 — 동일 패턴 함수의 라우팅 가능성 확인" 권고대로 미적용. | 6개 함수에 `config.path` 와일드카드 일괄 적용:<br>`export const config = { path: "/api/admin-milestone-revenue*" };` (5개 동일) + `milestone-settlement*`. 1줄씩 6 commit 또는 1 commit. 코드 본문 변경 불필요 (이미 `pathname.split("/")` 라우팅 구현됨). 머지 즉시 어드민 화면 6종 복구. |
| **H2** | Phase 28 §0 (EVENT_RANGE 매출연동 결산 처리) | `milestone-settlement.ts:214-233` `applyFormula` switch에 **`case "EVENT_RANGE"` 없음** → default 0 반환. `milestone-dashboard.ts:218-237` `calcIncentive` switch `case "EVENT_RANGE": return 0` 명시. `admin-milestone-revenue.ts:151` PUT은 `revenue_entries.amount`에 결정 금액 직접 저장 + status=VERIFIED 처리 | 매출연동 EVENT_RANGE 마일스톤(시드 `sm-006` 기업·기관 후원 협약 + `pm-005` 유족 순직 지원 컨설팅료)은 슈퍼어드민이 PUT으로 결정한 금액이 `revenue_entries.amount`에 저장됨. `calcSettlement`는 SUM(amount)로 가져오고 `applyFormula(formula, ...)`에 넘기는데 type=EVENT_RANGE는 default 0 반환 → **EVENT_RANGE 매출은 결산 시 인센티브 0원**. 명세 Phase 28 §0 "amount 컬럼을 EVENT_RANGE 결정 금액으로 재활용"의 의도는 결정 금액 = 보너스 금액. 비매출 EVENT_RANGE(non_revenue_achievements.event_range_amount)는 정상 처리되나 매출연동은 단절. 대시보드 "예상 인센티브"·결산 자동계산 둘 다 0원으로 표시 → 운영자가 자기 인센티브 누락 인지 못함. | 두 함수 모두 EVENT_RANGE 분기 추가:<br>```ts<br>case "EVENT_RANGE":<br>  return Math.round(current);  // amount 컬럼에 저장된 결정 금액 = 보너스<br>```<br>또는 매출연동 EVENT_RANGE는 thresholdEnabled=false라 base=current. `default`를 `return 0`에서 `return Math.round(current * (formula.rate || 1))`로 일반화도 가능. 매출연동 EVENT_RANGE는 동일 분기 여러 건 합산 가능성 있어 sum 그대로 보너스 인정이 명세 의도와 맞음. |
| **H3** | Phase 24 §2 함수 명세 일관성 | **함수 4개가 2쌍으로 중복 구현**:<br>① 정의 관리 — `milestone-definitions.ts`(segment ID, PATCH/DELETE) ↔ `admin-milestone-definitions.ts`(body.id PUT, ?id= DELETE, 변경 이력 INSERT 포함). admin-milestones.js는 milestone-definitions 호출, admin-milestone-settings.js는 admin-milestone-definitions 호출.<br>② 역할 배정 — `milestone-members.ts`(PATCH /:id/role) ↔ `admin-milestone-role-assign.ts`(PUT body.memberId+milestoneRole). admin-milestones.js는 milestone-members 호출, admin-milestone-settings.js는 admin-milestone-role-assign 호출. | **이력 일관성 깨짐**: admin-milestones.js(워크스페이스 쪽)로 정의를 수정하면 `milestone-definitions.ts` PATCH 경로로 들어가 `milestone_definition_history` INSERT가 안 됨(milestone-definitions.ts:88-108 R32-P0-MS-C3 fix는 drizzle update만 하고 history 인서트 코드 없음). admin-milestone-settings.html에서 같은 정의 수정하면 admin-milestone-definitions.ts:158-189 fieldMap으로 history INSERT 함. 어느 경로를 썼는지에 따라 이력 누락 → 변경 추적 신뢰성 깨짐.<br>또한 H1과 결합하면 milestone-definitions.ts·milestone-members.ts는 와일드카드 미적용 시 라우팅 실패. admin-milestones.js의 정의·역할 관리가 통째로 깨짐(현재 admin-milestone-settings.html은 body.id 방식이라 라우팅 영향 없음 — 일부만 동작) | 옵션 A(통합·권장): admin-milestones.js를 admin-milestone-definitions·-role-assign 엔드포인트로 전환 → milestone-definitions PATCH 핸들러·milestone-members PATCH 핸들러 제거(혹은 GET만 남김). 단일 진입점에 변경 이력 통합. UI 1곳 fetch 변경 + 함수 2개에서 PATCH/DELETE 분기 삭제 (~30줄).<br>옵션 B(유지·동기화): milestone-definitions.ts PATCH에 history INSERT 동일 로직 복사 (~40줄) + milestone-members PATCH는 milestone-role-assign과 동일 응답 키 보장. 함수는 유지하나 행위만 동기화. |

---

## §3 신규 M 우선순위 (표)

| ID | 명세 §위치 | 현재 상태 | 누락·결함 | 권장 해결 |
|---|---|---|---|---|
| **M1** | Phase 24 §1.1 quarters.status (cron SETTLED 전환) | `cron-milestone-quarter.ts:37-40` ENDED→SETTLED 조건: `quarterly_settlements.status NOT IN ('PAID', 'APPROVED')` 가 0개일 때 SETTLED. | 명세 의도는 모든 결산 **PAID**이어야 SETTLED. 현재는 APPROVED 단계에서도 SETTLED 전환 가능 → 슈퍼어드민이 PAID(지급 완료) 처리 전에 분기가 SETTLED로 마감 → 추후 지급 누락 위험 + cron-toss-billing 같은 후속 처리 트리거 누락. | `unpaidRows`의 NOT IN 조건에서 'APPROVED' 제거: `WHERE quarter_id = ${q.id} AND status NOT IN ('PAID')`. 즉 PAID만 100%일 때만 SETTLED. |
| **M2** | Phase 24 §2 milestone-definitions.ts PATCH (R32-P0-MS-C3 fix 후) | `milestone-definitions.ts:91-97` allowed 키 12개 + `if (key in body) patch[key] = body[key]`. null·undefined 검증 없음 + drizzle update().set()로 직접 전달. | `body = { name: null }`이면 patch.name = null로 drizzle 전송 → `name varchar NOT NULL` constraint 위반 → 500 + 사용자에게 raw SQL 에러 노출. `bonusFormula: "{invalid"` 같은 잘못된 JSON 문자열은 drizzle이 jsonb로 받지 못해 500. round2에서 admin-milestone-definitions.ts:120 처럼 `JSON.stringify(...JSON.parse(body.bonusFormula))` 검증 패턴 적용되어 있었으나 R32-P0-MS-C3 fix에는 누락 | NOT NULL 필드 검증 추가:<br>```ts<br>if ('name' in body && (!body.name || typeof body.name !== 'string')) return Response.json({ok:false,error:"name은 빈 문자열 불가"},{status:400});<br>if ('bonusFormula' in body) { try { patch.bonusFormula = typeof body.bonusFormula === 'object' ? body.bonusFormula : JSON.parse(body.bonusFormula); } catch { return Response.json({ok:false,error:"bonusFormula JSON 형식 오류"},{status:400}); } }<br>```<br>+ allowed 키별 typeof 검증 |
| **M3** | Phase 24 §2 milestone-definitions.ts PATCH 변경 이력 | R32-P0-MS-C3 fix(drizzle update().set())로 변경됐으나 `milestone_definition_history` INSERT 누락. admin-milestone-definitions.ts:158-189에는 fieldMap 16개 + INSERT 루프 존재 | admin-milestones.js로 정의 수정 시 변경 이력 미기록 → admin-milestone-settings.html "변경 이력" 탭에 누락된 변경 표시 안 됨. R29-MS-GAP1-E 도입한 이력 추적이 H3와 결합되어 일부만 동작. | H3 옵션 A(통합)로 admin-milestones.js를 admin-milestone-definitions 호출로 전환하면 자동 해결. 옵션 B(유지)면 milestone-definitions.ts PATCH 직전에 oldRows 조회 + fieldMap 복사. |
| **M4** | Phase 28 §3-B-3 milestone-settlement.ts 결산 제출 | `milestone-settlement.ts:71-95` UPSERT 트랜잭션 없음. SELECT existing 후 분기 별도 INSERT/UPDATE. 동시성 시 두 요청이 동시에 SELECT → 둘 다 ex=null → 둘 다 INSERT → `quarter_member_uq` UNIQUE 위반 1건 + 다른 1건 통과 | 결산 제출은 1회성이라 충돌 가능성 낮으나 어드민이 더블 클릭하거나 자동계산 후 즉시 제출 시 잠재. UNIQUE 위반 500이 사용자에게 노출됨 + 운영자가 "이미 제출됨" 메시지 못 받음. | INSERT 시 `ON CONFLICT (quarter_id, member_id) DO UPDATE SET ... RETURNING *`로 UPSERT 단일 쿼리화. 또는 try-catch로 unique 위반 시 분기 후 UPDATE 시도. |

---

## §4 신규 부분 구현 (🟡)

| ID | 항목 | 현재 상태 | 부족분 |
|---|---|---|---|
| **🟡A** | 역할 배정 UI 이중화 (H3 관련) | admin-milestones.js의 "담당 역할 설정" 탭 + admin-milestone-settings.html의 별도 화면. 둘 다 동작 (한쪽은 milestone-members, 한쪽은 admin-milestone-role-assign) | 명세 §3.3 admin.html 서브메뉴 "👥 담당 역할 설정" 한 곳만 명시. 두 곳에서 같은 데이터 수정 가능 → 한쪽 화면이 캐시된 milestoneRole 표시 + 다른 곳 변경 → UI 새로고침 전까지 불일치. UX 혼동. H3 통합으로 자연 해결. |

---

## §5 round2 미처리 항목 status 갱신

> docs/gap/2026-05-19-round2-ms.md §2~§4 항목 — round3 시점 재확인.

| ID | round2 분류 | round3 status | 비고 |
|---|---|---|---|
| H1 base_salary 컬럼 부재 | H | **✅ 해소** (R32-P0-MS-C4: 마이그 호출 + schema 활성화 + CSV PASS 검증) | f1c7450·67ef291 |
| H2 milestone-definitions PATCH sql.raw | H | **✅ 해소** (R32-P0-MS-C3: drizzle update().set() ORM + raw SELECT 재조회) | 67ef291 |
| H3 milestone-quarters PATCH sql.raw | H | **✅ 해소** (R32-P0-MS-C2: sql 템플릿 합성) | 67ef291 |
| H4 milestone-nonrevenue/select sql.raw | H | **✅ 해소** (R32-P0-MS-C1: sql 템플릿 + ANY(int[])) | 67ef291 |
| **H5 EVENT_RANGE 단위 일관성** | H | **❌ 미해소** | UI 라벨 `만원` 표시 + 시드 단위 결정 미진행. 시드 데이터 제외 정책으로 코드 변경만 가능 — 현재 정정 안 됨 |
| M1 CSV roleLabel lowercase | M | **✅ 해소** (R32-P0-MS-C4-2: 대소문자 동시 매핑) | 67ef291 |
| **M2 AI 함수 이중화** (classify·coach) | M | **❌ 미해소** | `ai-milestone-classify.ts` dead code 그대로, `ms-ai-coaching.ts` 잔존. round3 H3와 동일 패턴 — 4쌍 이중화로 확대 |
| **M3 EVENT_RANGE UI 위치 (admin-milestone-settings)** | M | **❌ 미해소** | admin-milestone-settings.html에 매출 검증 탭 자체 없음. workspace-milestones에서만 처리. 명세 §2-A-1과 화면 위치 불일치 |
| **M4 milestone:* 권한 시드 8개** | M | **❌ 미해소** | grep 결과 schema·DB·UI 어디에도 milestone:* 키 미등록. Phase 24 §1.3 명세 미적용. 권한 정책 페이지에 토글 항목 누락 |
| **M5 settlementDate 30일 vs 14일** | M | **❌ 미해소** | admin-milestone-settlement.ts:205 `endMonthDate.getMonth() + 2, 0` 그대로. UI 안내 "10~14일"과 자동 생성 30일 불일치 |
| **M6 quarterApplicable 검증 누락** | M | **❌ 미해소** | milestone-nonrevenue.ts POST에 quarter 비교 검증 없음. sm-q1-* 마일스톤을 Q2 분기에 제출 가능 |
| **M7 admin-milestone-settlement.ts:108-123 sql.raw setClauses** | M | **❌ 미해소** | escape 패턴 + sql.raw 합성 그대로. SQL injection 표면 좁히기는 OK이나 보수 권장 패턴 아님 |
| 🟡1 EVENT_RANGE 범위 라벨 | 🟡 | 🟡 유지 (H5 미해소 영향) | |
| 🟡2 AI 매출 자동 분류 | 🟡 | 🟡 유지 (M2 미해소 영향) | |
| 🟡3 자가평가 코칭 | 🟡 | 🟡 유지 (M2 미해소 영향) | |

**round2 미처리 잔존 카운트**: H 1 + M 6 + 🟡 3 = **10건**.

---

## §6 R32-P0 효과·부작용

### 6.1 R32-P0-B Critical 4건 (commit 67ef291) — 모두 안착

| 항목 | 적용 위치 | 결과 |
|---|---|---|
| C1 milestone-nonrevenue/select | sql 템플릿 + ANY(int[]) + 본인+개수 일치 검증 | ✅ R32-P0 verify에서 라우팅 한계로 PARTIAL이었으나 FIX-2(와일드카드 적용) 후 정상 동작 가능 |
| C2 milestone-quarters PATCH | sql 템플릿 분기 합성 (status·settlementDate::date) | ✅ verify A2 PASS (?id= 쿼리 fallback) — 다만 segment 라우팅은 H1 라우팅 BUG로 여전히 깨짐 |
| C3 milestone-definitions PATCH | drizzle update().set() + raw SELECT 재조회 | ✅ FIX-1(와일드카드) 후 segment·query 둘 다 동작. 다만 history INSERT 누락(M3) + null 검증 누락(M2) 부작용 |
| C4 base_salary 마이그 + roleLabel | 컬럼 추가 + schema 활성화 + 대소문자 매핑 | ✅ verify A4 PASS — CSV 다운로드 200 정상 |

### 6.2 R32-P0-FIX-1·FIX-2 (commit bca7673)

| 항목 | 적용 | 부작용 |
|---|---|---|
| FIX-1 milestone-definitions DELETE `?id=` query fallback + path `*` 와일드카드 | ✅ | ✅ admin-milestones.js의 DELETE 동작 복구. PATCH도 segment·query 모두 동작 |
| FIX-2 milestone-nonrevenue path `*` 와일드카드 | ✅ | ✅ workspace-milestones.js의 select POST 동작 복구 |
| **잠재 부작용** | — | ❌ **R32-P0 verify §5.3 "후속" 권장 — 동일 패턴 6개 함수 라우팅 점검 미적용**. round3 H1로 격상 |

### 6.3 base_salary 활성화 후속

- ✅ schema.ts:311 `baseSalary: numeric("base_salary", { precision: 15, scale: 2 }).default("0").notNull()` 정상 등록
- ✅ admin-milestone-settlement-export.ts:35 SELECT `m.base_salary` 정상 동작
- ⚠️ **미해소 사항**: 회원별 base_salary 실제 입력 UI 없음. 현재 모든 회원 0원 → CSV 기본연봉 열이 항상 0. 슈퍼어드민이 회원별 기본연봉 입력하는 화면 신설 또는 admin-members.html에 컬럼 추가 필요. round3 별도 M으로 격상하기엔 운영 미시작 단계라 부분 구현(🟡)으로 분류 가능 — 본 라운드는 미보고(제외 정책 §8 "급여 자동 push" 인접).

### 6.4 회귀 영향

- 부재 — R32-P0 verify §4 회귀 점검 14건 ALL PASS 확인. R29 P1·P2·R30 KST 무영향.

---

## §7 ✅ 완전 구현 카운트 (R32-P0 후 갱신)

round2 §5 + R32-P0-B fix + base_salary 활성화 + FIX-1·FIX-2 결과 (총 24개+):

**알림 16종**: round2와 동일 — `cron-milestone-quarter.ts`·`admin-milestone-*.ts`·`milestone-revenue.ts`·`milestone-nonrevenue.ts`·`milestone-settlement.ts` 각 핸들러에 createNotification/notifyMany/notifyAllSuperAdmins 통합 (R29-GAP-P2-M1 + R29-MS-GAP1-D/G/J)

**AI 4타입**: 자동 분류(ms-ai-classify) + 요약(handleSummary) + 이상 탐지(handleAnomaly) + 자가평가 코칭(handleCoach + ms-ai-coaching) + 다음 분기 추천(handleRecommend) — 단 일부 이중화는 H3·🟡A로 보고

**DB 스키마**: quarters·milestoneDefinitions·revenueEntries·nonRevenueAchievements·quarterlySettlements(holdReason 포함)·milestoneDefinitionHistory(R29-MS-GAP1-E) + members.milestoneRole + **members.baseSalary(R32-P0 신규)** 모두 정상

**핵심 워크플로우·UX**:
- ✅ HOLD 트랜지션 (SUBMITTED/REVIEWED → HOLD → REVIEWED 복귀)
- ✅ 결산 SUBMITTED/APPROVED/PAID 분기 매출·비매출 입력 차단
- ✅ D-N 카운트다운 배지 (분기 종료 임박)
- ✅ 비매출 N/2개 선택 배지 + 시각 강조 + 비활성
- ✅ 인센티브 breakdown 토글 (KPI 클릭)
- ✅ 결산 자동계산 / 자가평가 / AI 코칭 / 제출 (단 H1 라우팅 영향 — 도달 불가 잠재)
- ✅ 매출연동 분기 결산 정의 격리 (effectiveFrom/to)
- ✅ SI 공유 임계점 채널별 배분 (sm-001~003 — calcSISharedBonus)
- ✅ admin.html 슈퍼어드민 서브메뉴 그룹 (data-super-only="1")
- ✅ workspace.html #milestones 패널 임베드 + Phase 25 진행률 게이지
- ✅ admin-milestones.html iframe 4종 (#defs/#quarters/#settlements/#roles)
- ✅ EVENT_RANGE 입력 UI(workspace-milestones·admin-milestone-revenue) — 단 단위(H5)·이중 화면(M3)·결산 계산(H2) 미해소

**CSV export (Phase 28 §3-B-7)**:
- ✅ R32-P0-MS-C4 후 PASS — 8개 컬럼 + UTF-8 BOM + 한국어 라벨

---

## §8 제외 항목

> R29-GAP-B/C §6 정책 그대로 유지.

| 항목 | 사유 |
|---|---|
| 씨드 마이그 47개 (sm/pm/si 마일스톤) | Phase 24 시드 53개로 대체 가능 — R29 §6 제외 |
| AI 5종(자동분류·요약·이상·코칭·추천) 신규 추가 | 이미 코드 구현 완료 — R29 §6 제외 (이중화는 H3에서 별도 보고) |
| 분기 통계 차트·팀 랭킹 | 제외 정책 |
| 급여 시스템 자동 push | CSV export로 대체 (Phase 28 §3-B-7) |
| 회원별 base_salary 입력 UI | 운영 미시작 — 제도 정착 후 별도 페이즈 |

---

## §9 조사 메타데이터

| 항목 | 값 |
|---|---|
| 조사일 | 2026-05-19 |
| 조사자 | B 채팅 Opus 4.7 |
| 기준 시점 (트리거) | main @ fb13db7 (R32-P0-B 머지 + base_salary 활성화 직후) |
| 분석 범위 | origin/main HEAD bca7673(R32-P0-FIX-1·FIX-2 라우팅 패치)까지 반영 |
| 명세서 1차 소스 | docs/milestones/2026-05-19-phase24-milestone-performance.md (634줄) + phase28-performance-completion.md (313줄) |
| 검증 보고서 | docs/verify/2026-05-19-r32-p0.md (verify §5.3 "FIX-2 후속" 권고가 H1 격상의 1차 근거) |
| 이전 갭 보고서 | docs/gap/2026-05-19-round2-ms.md (round2 status 갱신) |
| 코드 2차 소스 | netlify/functions/milestone-*.ts (7개) + admin-milestone-*.ts (6개) + cron-milestone-quarter.ts + ai-milestone-*.ts (2개) + ms-ai-*.ts (2개) + migrate-* (이미 삭제됨) |
| 프론트 2차 소스 | public/workspace-milestones.html·.js(1115줄) + admin-milestones.html·.js(579줄) + admin-milestone-settings.html·.js + admin.html §성과관리 그룹 |
| DB | db/schema.ts §3419~3526 (Phase 24) + §311(baseSalary R32-P0) + R29-MS-GAP1 history |
| 분석 시간 | 약 1.5h |

**다음 라운드 권장**:
- **R33-P0(긴급)**: H1 라우팅 와일드카드 5건 일괄 fix (`config.path` 1줄씩 6 commit 또는 1 commit). 코드 변경 6줄. 머지 즉시 어드민 6종 액션 복구
- **R33-P0(긴급)**: H2 EVENT_RANGE 계산 — applyFormula·calcIncentive 2함수에 case 추가 (~6줄). 시드 sm-006·pm-005 매출 결산 0원 BUG 해결
- **R33-OP-2 (Swain 결정)**: H5 EVENT_RANGE 단위 통일 + M4 milestone:* 권한 시드 — 의사결정 필요
- **R34(추후)**: H3 함수 이중화 통합 (옵션 A 권장 — 30줄) + M1·M2·M3·M5·M6·M7·🟡A 묶음
- **R34-P0(별건)**: 회원별 base_salary 입력 UI (admin-members.html 컬럼 추가)

---

> 본 보고서는 분석만 — fix 코드 수정 없음. fix는 별도 라운드(R33-P0 등) 또는 메인 결정 후 라운드 트리거.
