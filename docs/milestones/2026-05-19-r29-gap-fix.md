# R29-GAP — 근태·성과 갭 수정 라운드 설계서

> 작성: 2026-05-19 / 메인 (Opus 4.7)
> 출처: 루트 `gap.md` (A·B 정밀 갭 분석 통합본)
> 라운드 분할: **P1 (H 우선순위) → P2 (M 우선순위)**. L 우선순위는 [REMAINING_WORK.md](../REMAINING_WORK.md) 인벤토리로 이관.
> 머지 순서: B 백엔드 → A 프론트 → 메인 머지 → C 검증.

---

## §0 목표

- 근태 H 5건 + 성과 H 5건 (= P1) 핵심 워크플로우 단절 해소 → 어드민이 휴가·수정 결재·결산 검토를 화면에서 직접 수행 가능
- 근태 M 5건 + 성과 M 3건 (= P2) 운영 효율·일관성 정리 → 화면 중복·진입점 부재·응답 키 표준화
- C 라이브 점검 이슈 2건 흡수
  - 이슈-1 분기 부재 → P1 §1 운영 액션
  - 이슈-2 반차 모델 이중화 → L (REMAINING_WORK)

---

## §1 운영 액션 (Swain 직접 — 코드 외)

| # | 액션 | 위치 |
|---|---|---|
| OP-1 | 2026 Q2 분기 등록 (현재 ACTIVE 분기 0) | https://tbfa.co.kr/admin-milestone-settings.html → 분기관리 탭 → 추가 |
| OP-2 | (선택) cron-milestone-quarter 동작 확인 — 다음 분기 자동 생성 누락 원인 점검 | 운영 또는 P2 M 영역 |

P1·P2 코드 작업에 종속되지 않음.

---

## §2 라운드 분할

### P1 — H 우선순위 (10건)
| 영역 | 담당 | 항목 수 | 결과물 |
|---|---|---|---|
| 근태 | A (Opus) | 5 | 어드민 결재 UI 3건·CRUD 경로 정정·요청자 가드 분리 |
| 성과 | B (Opus) | 5 | 결산·정의 응답 키 정합·EVENT_RANGE 필드 노출 |

### P2 — M 우선순위 (8건)
| 영역 | 담당 | 항목 수 | 결과물 |
|---|---|---|---|
| 근태 | A (Opus) | 5 | 어드민 화면 중복 정리·진입점 보강·cron 명세 정합 |
| 성과 | B (Opus) | 3 | 임계점 알림 그룹화·정의 API 표준화·결산 단건 응답 |

**P1 머지·C 검증 PASS 확인 후 P2 트리거 발사.**

---

## §3 충돌 분석

| 파일 | A (근태) | B (성과) | 충돌 |
|---|---|---|---|
| `public/js/admin-workspace-management.{html,js}` | 수정·신규 탭 | — | 0 |
| `public/js/admin-attendance-settings.{html,js}` | 수정 (P2) | — | 0 |
| `public/js/admin-milestones.js` | — | 수정 | 0 |
| `public/js/admin-milestone-settings.js` | — | 수정 가능 | 0 |
| `netlify/functions/admin-att-*.ts` | 수정·신규 | — | 0 |
| `netlify/functions/admin-milestone-*.ts` | — | 수정 | 0 |
| `lib/operator-guard.ts` | 신규 | — | 0 |
| `netlify/functions/att-*.ts` (직원 9개) | import 교체 | — | 0 |

**A·B 워크트리 분리 + 파일 도메인 분리 → 충돌 0.**

---

## §4 DB 마이그레이션

**불필요.** P1·P2 모두 코드 수정·신규 함수·신규 헬퍼·UI 추가만. schema.ts 변경 없음.

---

## §5 P1 작업 카탈로그 (체크리스트 단위)

### §5.1 A 채팅 (근태 H1~H5) — 어드민 결재·UI·가드

영역 라벨: **프론트(주) + 백엔드(부분 신규 1개)**

#### A-H1 수정요청 결재 경로 정합 (3건)

**문제**: 프론트 `admin-workspace-management.js`가 존재하지 않는 `/api/admin-att-amend-requests` REST 경로를 호출 → 404. 백엔드는 `/api/admin-att-correction-review` (POST만) 완비.

체크리스트:
- [ ] B작업 일부 — `netlify/functions/admin-att-correction-review.ts`에 GET 핸들러 추가
  - [ ] `req.method === "GET"` 분기
  - [ ] `status=PENDING` 쿼리 (기본값) — `attCorrections.status = 'PENDING'`
  - [ ] `members` 조인 — `name`, `email`, `phone` 노출
  - [ ] 응답: `{ ok: true, data: { corrections: [...] } }`
  - [ ] 정렬: `submittedAt ASC` (오래된 신청부터)
  - [ ] LIMIT 100
- [ ] `admin-workspace-management.js:140` GET 경로 정정
  - [ ] `/api/admin-att-amend-requests?status=PENDING` → `/api/admin-att-correction-review?status=PENDING`
  - [ ] 응답 키 fallback — `res.data?.data?.corrections || res.data?.corrections || []`
  - [ ] 행 렌더 키 — `memberName`, `targetDate`, `correctionType`, `requestedCheckIn`, `requestedCheckOut`, `reason`
- [ ] `admin-workspace-management.js:171` 승인 호출 정정
  - [ ] `POST /api/admin-att-correction-review` body `{ requestId, action: "APPROVED", note: "" }`
- [ ] `admin-workspace-management.js:179` 반려 호출 정정
  - [ ] `POST /api/admin-att-correction-review` body `{ requestId, action: "REJECTED", note: reason || "" }`
- [ ] toast 메시지: "수정요청 승인/반려 완료"
- [ ] 처리 후 목록 새로고침
- [ ] 캐시버스터 `admin-workspace-management.js?v=N+1` 갱신

#### A-H2 휴가 결재 UI 신설

**문제**: 백엔드 `/api/admin-att-leave-review` 완비, UI 없음.

체크리스트:
- [ ] B작업 일부 — `netlify/functions/admin-att-leave-review.ts`에 GET 핸들러 추가
  - [ ] `req.method === "GET"` 분기
  - [ ] `status=PENDING` 쿼리 (기본값) — `attLeaves.status = 'PENDING'`
  - [ ] `members` + `attLeaveTypes` 조인 — 이름·휴가종류명·일수 노출
  - [ ] 응답: `{ ok: true, data: { leaves: [...] } }`
- [ ] `admin-workspace-management.html`에 "휴가 결재" 탭 버튼·섹션 신설
  - [ ] `data-tab="leaves"` 버튼 추가 (기존 탭 옆)
  - [ ] `<section id="tab-leaves" hidden>` 컨테이너
  - [ ] 내부: `<div id="leavesPending">`, `<table id="leavesTable">`
- [ ] `admin-workspace-management.js`에 `initLeavesTab()` 함수 신설
  - [ ] 탭 클릭 시 `_awmLvInit` 가드로 1회 초기화
  - [ ] `loadLeavesPending()` 호출 — GET `/api/admin-att-leave-review?status=PENDING`
  - [ ] 행 렌더: 신청자·휴가종류·시작일·종료일·일수·사유·신청일
  - [ ] 승인 버튼 → POST `/api/admin-att-leave-review` `{ leaveId, action: "APPROVED" }`
  - [ ] 반려 버튼 → 사유 prompt + POST `{ leaveId, action: "REJECTED", reason }`
- [ ] 처리 후 잔여휴가 자동 차감 동작 확인 (백엔드 기존 로직)
- [ ] 처리 후 신청자에게 인앱 알림 발송 확인 (백엔드 기존 로직)
- [ ] toast "휴가 신청 승인/반려 완료" + 목록 새로고침
- [ ] 캐시버스터 갱신

#### A-H3 잔여휴가 관리 UI 신설

**문제**: 백엔드 `/api/admin-att-leave-balances` 완비(GET/PUT), UI 없음.

체크리스트:
- [ ] `admin-workspace-management.html`에 "잔여휴가" 탭 추가
  - [ ] `data-tab="balances"` 버튼
  - [ ] `<section id="tab-balances" hidden>`
  - [ ] 연도 선택 select (현재 연도 기본)
  - [ ] 검색 input (회원명·이메일)
- [ ] `initBalancesTab()` 신설
  - [ ] GET `/api/admin-att-leave-balances?year=YYYY`
  - [ ] 회원별 행 렌더: 이름·이메일·휴가종류·총일수·사용일수·잔여일수
  - [ ] 조정 모달 — 일수 ±N·사유 입력
  - [ ] PUT `/api/admin-att-leave-balances` `{ memberUid, leaveTypeId, year, deltaDays, reason }`
- [ ] 신규 입사자 일괄 부여 버튼 (보너스) — 본인 입사일 기준 자동 부여 트리거 (cron-att-leave-auto 수동 재실행 또는 안내 메시지)
- [ ] toast "조정 완료" + 행 갱신
- [ ] 캐시버스터 갱신

#### A-H4 거점·휴가종류·공휴일 CRUD 경로 정합

**문제**: 프론트는 REST `/{id}` 호출 → 백엔드는 `?id=` query 또는 단수 경로. Netlify path segment 매칭 미지원 → 404.

체크리스트:
- [ ] 거점 (workplaces)
  - [ ] `admin-workspace-management.js:336` DELETE 경로 정정 — `/api/admin-att-workplaces/${id}` → `/api/admin-att-workplaces?id=${id}`
  - [ ] line 376 PUT 경로 정정 — 같은 방식
  - [ ] 백엔드 `admin-att-workplaces.ts` GET·POST·PUT·DELETE가 `?id=` 사용 확인 (이미 그러함)
  - [ ] 중복 파일 `admin-att-workplace.ts` (단수) 제거 또는 보존 결정 — **단수는 삭제**(미사용 + 명세 단일화)
- [ ] 휴가종류 (leave-types)
  - [ ] `admin-workspace-management.js:499` DELETE 정정 — `/api/admin-att-leave-types/${id}` DELETE → `/api/admin-att-leave-types` DELETE `{ body: { id } }` (백엔드가 body로 받음)
  - [ ] line 519 PUT 정정 — body에 `id` 포함
- [ ] 공휴일 (holidays)
  - [ ] `admin-workspace-management.js:586` DELETE 정정 — `/api/admin-att-holidays/${id}` DELETE → `/api/admin-att-holidays?id=${id}` DELETE
- [ ] 각 자원 PUT/DELETE 후 목록 자동 새로고침
- [ ] toast "거점/휴가종류/공휴일 수정·삭제 완료"
- [ ] 캐시버스터 갱신

#### A-H5 직원 API requireOperator 분리

**문제**: `att-*` 직원 API 9개가 `requireAdmin` 사용 → `member.type === 'admin'` 강제 → 운영자(operator) 출퇴근 불가 + 블랙 직원 차단 누락.

체크리스트:
- [ ] B작업 — `lib/operator-guard.ts` 신규 (lib/admin-guard.ts 수정 금지 — §5 제약)
  - [ ] `requireOperator(req): Promise<{ok:true, ctx:{member}} | {ok:false, res}>`
  - [ ] `lib/auth.ts`의 일반 사용자 JWT 검증 사용 (verifyUserJwt 등 기존 헬퍼 재사용)
  - [ ] members에서 `uid` 일치하는 행 SELECT
  - [ ] 검증 순서: 존재 → status active → operatorActive=true (또는 type=admin) → BLACKLIST 차단
  - [ ] 실패 시 응답 표준 `unauthorized()` / `forbidden()`
  - [ ] 반환 ctx 구조는 `{ member }`로 단순화 (기존 호출부에서 `auth.ctx.member` 그대로 사용 가능)
- [ ] 9개 함수 import 교체 — `requireAdmin` → `requireOperator`
  - [ ] `att-checkin.ts`
  - [ ] `att-checkout.ts`
  - [ ] `att-my-status.ts`
  - [ ] `att-schedule-today.ts`
  - [ ] `att-leave-request.ts`
  - [ ] `att-my-leaves.ts`
  - [ ] `att-correction-request.ts`
  - [ ] `att-amend-request.ts`
  - [ ] `att-export.ts`
- [ ] 각 파일에서 `auth.ctx.member.id`·`auth.ctx.member.uid` 참조 유지
- [ ] 블랙 차단 통합 — `member.status === 'blacklisted'`도 차단 (lib/auth.ts requireActiveUser 패턴 일부 차용)
- [ ] 운영자가 출퇴근 가능한지 로컬 검증 (운영자 계정 type=operator, operatorActive=true)
- [ ] `node --check` 또는 `tsc --noEmit` 통과 확인

#### A 영역 마무리 체크
- [ ] `git status` clean 후 commit
- [ ] feature 브랜치명: `feature/r29-gap-p1-att-admin`
- [ ] push 전 Swain에게 확인 요청 (자율주행 정책 §6.17)
- [ ] 머지는 메인이 처리

---

### §5.2 B 채팅 (성과 H1~H5) — 어드민 응답 키 정합 + EVENT_RANGE

영역 라벨: **프론트(주) + 백엔드(부분 1건)**

#### B-H1 결산 응답 키 정합

**문제**: 백엔드 `/api/admin-milestone-settlement` 응답 키 `year/quarter/revenueLinkedTotal/nonRevenueTotal/totalBonus` ↔ 프론트 `admin-milestones.js` 사용 키 `quarterYear/quarterQ/revenueIncentive/nonRevenueBonus/totalAmount` 어긋남 → 분기·금액 컬럼 모두 "-".

체크리스트:
- [ ] `public/js/admin-milestones.js:352` 분기 컬럼 정정
  - [ ] `s.quarterYear ? s.quarterYear + '년 Q' + s.quarterQ : '-'` → `s.year ? s.year + '년 Q' + s.quarter : '-'`
- [ ] line 353 revenueIncentive → revenueLinkedTotal
- [ ] line 354 nonRevenueBonus → nonRevenueTotal
- [ ] line 355 totalAmount → totalBonus
- [ ] 동일 파일 내 다른 위치 grep으로 추가 4개 키 모두 정정 (혹시 다른 함수에서도 사용 중인지)
  - [ ] `grep "quarterYear\\|quarterQ\\|revenueIncentive\\|nonRevenueBonus\\|totalAmount" public/js/admin-milestones.js`
  - [ ] 발견되는 모든 위치 정정
- [ ] 금액 표시 함수 `amFmt`가 number/null 모두 안전 처리 확인
- [ ] 캐시버스터 갱신

#### B-H2 정의 목록 응답 키 정합

**문제**: 프론트가 `res.data?.data?.definitions || res.data?.definitions`만 검사. 실제 응답 키는 `milestones` 또는 직접 배열.

체크리스트:
- [ ] `admin-milestones.js:87` fallback 확장
  - [ ] `res.data?.data?.milestones || res.data?.milestones || res.data?.data?.definitions || res.data?.definitions || (Array.isArray(res.data?.data) ? res.data.data : []) || []`
- [ ] 화면 이중성 정리 — `admin-milestone-settings.html`(slug)가 정상 동작이므로, `admin-milestones.js`의 정의 탭 표시 후 둘 다 동작 확인
- [ ] 빈 배열 시 메시지 유지 — "등록된 마일스톤이 없습니다"
- [ ] 정의 개수 표시 UI 갱신 확인

#### B-H3 정의 등록·수정 모달 키 일치

**문제**: 백엔드 `admin-milestone-definitions.ts` 요구 `targetMilestoneRole`·`thresholdEnabled` 등, 프론트는 옛 `milestoneRole`·`threshold 객체`.

체크리스트:
- [ ] 모달 열기 시 기존 값 표시 (line 150 부근)
  - [ ] `d.milestoneRole` → `d.targetMilestoneRole`
  - [ ] `d.threshold` 객체 분해 → `d.thresholdEnabled` / `d.thresholdValue` / `d.thresholdAmount`
  - [ ] threshold 관련 input id 정합 (HTML도 함께)
- [ ] 저장 시 body 키 정합 (line 195 부근)
  - [ ] `milestoneRole` → `targetMilestoneRole`
  - [ ] body 필드: `code`, `name`, `category`, `targetMilestoneRole`, `bonusFormula`, `thresholdEnabled`, `thresholdValue`, `thresholdAmount`, `description`, `active`
  - [ ] 필수 4개(code/name/category/targetMilestoneRole) 미입력 차단
- [ ] 응답 검증 — `res.ok && res.data?.ok`
- [ ] 저장 후 목록 새로고침
- [ ] 수정·삭제 흐름 동일 키 확인
- [ ] line 350·530·539 등 그 외 milestoneRole 사용처 grep 후 일관 정정

#### B-H4 EVENT_RANGE bonusFormula 응답 매핑

**문제**: `admin-milestone-revenue.ts` SELECT에 `md.bonus_formula` 포함되지만 응답 매핑에서 누락 → 프론트가 EVENT_RANGE 판정 불가 → 범위 UI 미표시.

체크리스트:
- [ ] `netlify/functions/admin-milestone-revenue.ts` GET 목록 응답 매핑에 `bonusFormula: r.bonus_formula` 추가
  - [ ] 응답 row 객체에 `bonusFormula`·`milestoneCategory`·`milestoneName` 모두 노출
- [ ] 단건 GET 응답에도 동일 추가
- [ ] `admin-milestones.js` 매출 검증 행 렌더에서 EVENT_RANGE 분기
  - [ ] `if (entry.milestoneCategory === 'EVENT_RANGE')` → 범위 안내 표시
  - [ ] 안내 텍스트: `범위: ${formula.minAmount}~${formula.maxAmount}만원`
  - [ ] 금액 input 노출 (PUT 시 amount 결정용)
  - [ ] 일반 행과 동일 승인/반려 버튼 + amount 포함
- [ ] PUT 호출 시 body에 `amount` 포함 검증 (이미 백엔드 `R29-MS-GAP1-I` 범위 검증 구현됨)
- [ ] 캐시버스터 갱신

#### B-H5 결산 화면 분기 드롭다운

**문제**: H1과 동일 원인 — `s.quarterYear`/`s.quarterQ` 코드가 옵션 텍스트 생성 → 빈 옵션.

체크리스트:
- [ ] `admin-milestones.js` 분기 옵션 생성 코드 grep
  - [ ] `quarterYear`·`quarterQ` 사용 위치 모두 → `year`·`quarter`
- [ ] 옵션 텍스트: `${s.year}년 Q${s.quarter}`
- [ ] 옵션 value: settlement id 또는 quarterId
- [ ] 기본 선택 = ACTIVE 분기 (현재 없으면 최신 SETTLED)
- [ ] 분기 변경 시 결산 목록 자동 새로고침

#### B 영역 마무리 체크
- [ ] `git status` clean 후 commit
- [ ] feature 브랜치명: `feature/r29-gap-p1-milestone-keys`
- [ ] push 전 Swain 확인
- [ ] 머지는 메인이 처리

---

## §6 P2 작업 카탈로그 (P1 머지 PASS 후 발사)

### §6.1 A 채팅 (근태 M1~M5)

체크리스트 (요약 — 세부는 P2 발사 시 트리거 본문에 박음):
- [ ] M1 어드민 근태 화면 2개 중복 정리 — `admin-workspace-management.html`(P26)와 `admin-attendance-settings.html`(P27) 중 한쪽 표준화
  - [ ] 결정 안: 결재/잔여/거점/정책/휴가종류/공휴일 → `admin-workspace-management.html`
  - [ ] 재택보고서/근무형태 → `admin-attendance-settings.html`
  - [ ] 중복 3탭(거점·휴가종류·정책) 한 곳만 유지·다른 곳 제거
- [ ] M2 `admin-attendance-settings.html` 진입점 — `admin-hub.html`에 카드 추가
- [ ] M3 cron-att-evening 명세 정합 — 명세 §6 EARLY_LEAVE 자동 생성 vs 현재 슈퍼어드민 통보만. 정책 결정 후 문서 갱신
- [ ] M4 amend/correction 함수 중복 정리 — `att-amend-*` 별칭 함수를 `att-correction-*`로 통일하거나 양쪽 유지 명문화
- [ ] M5 cron-att-leave-auto 만근 기준 — 명세 "결근 0건"과 구현 "ABSENT+LATE+EARLY_LEAVE 0건" 정합 (정책 결정)

### §6.2 B 채팅 (성과 M1~M3)

체크리스트:
- [ ] M1 임계점 도달 알림 — 입력자 그룹 → 담당 어드민(SM/PM/SI) 그룹화
  - [ ] `notification` 발행 시점에 entry.member_id의 milestone_role → milestone_role_assignments 에서 담당 admin 조회 → 그 admin에게 알림
- [ ] M2 정의 API 응답 일관성 — `milestone-definitions`·`admin-milestone-definitions` 두 함수 모두 `{ data: { milestones: [...] } }`로 통일
- [ ] M3 분기 결산 GET 단건 응답 — `quarterId` 지정 시 `{ settlement }` 단건 추가 노출 (배열 함께 유지로 회귀 0)

---

## §7 L 우선순위 → REMAINING_WORK 이관

| 항목 | 출처 | 사유 |
|---|---|---|
| 반차 모델 이중화 정리 | C 이슈-2 | allowHalfDay 컬럼·옛 반차 행 정리 (운영 영향 0, 미관 개선) |
| AI 흐름파악 시각화·코칭 자동화 | 근태 L | §6 필터 제외 |
| 통계 탭 주별 트렌드·히트맵 | 근태 L | 제외 정책 |
| CSV export 컬럼 커스터마이즈 | 근태 L | 제외 정책 |
| 근태 이상 탐지 ML | 근태 L | 제외 정책 |
| AI 일일요약 프롬프트 고도화 | 근태 L | 제외 정책 |
| 씨드 마이그 47개 | 성과 L | Phase 24 53개 시드 존재로 대체 가능 |
| AI 5종 (분류·요약·이상·코칭·추천) | 성과 L | 이미 구현됨 |
| 분기 통계 차트·팀 랭킹 | 성과 L | §6 필터 제외 |
| 급여 자동 push | 성과 L | CSV export로 대체 |

---

## §8 머지 순서·캐시버스터

P1 머지:
1. B `feature/r29-gap-p1-milestone-keys` → main
2. A `feature/r29-gap-p1-att-admin` → main
3. `admin-workspace-management.js?v=`·`admin-milestones.js?v=`·`admin-attendance-settings.js?v=` 모두 +1
4. push 완료 후 C 검증 트리거 발사

P2는 P1 PASS 후 동일 패턴.

---

## §9 C 검증 시나리오 (P1 완료 후)

### Q1. 수정요청 결재 PASS
- 직원 계정으로 수정요청 1건 작성 (`workspace-attendance.html`)
- 어드민 `admin-workspace-management.html` → 수정요청 탭에서 행 표시 확인
- 승인 클릭 → toast·`att_records` 반영·신청자 알림 확인
- 반려 클릭 → toast·반려 사유 노출 확인

### Q2. 휴가 결재 PASS
- 직원 계정으로 휴가 신청
- 어드민 → 휴가 결재 탭 행 표시 확인
- 승인 → 잔여휴가 자동 차감 확인·LEAVE 일자 UPSERT 확인
- 반려 → 사유 prompt 동작 + 신청자 알림

### Q3. 잔여휴가 관리 PASS
- 어드민 → 잔여휴가 탭 행 표시
- 조정 모달 +3일 / -1일 동작
- PUT 후 행 즉시 반영

### Q4. 거점·휴가종류·공휴일 CRUD PASS
- 거점 PUT·DELETE 정상 동작 (404 사라짐)
- 휴가종류 DELETE (body id) 정상
- 공휴일 DELETE (?id= query) 정상

### Q5. 운영자 출퇴근 PASS
- 운영자(type=operator) 계정으로 `workspace-attendance.html` 진입
- 출근·퇴근 버튼 동작 확인 (이전: requireAdmin로 차단됨)
- 블랙리스트 직원으로 출근 시도 → 403 차단 확인

### Q6. 결산 화면 분기/금액 PASS
- 어드민 → 결산 화면 진입
- 분기 컬럼 "2025년 Q2" 등 정상 표시 (이전: "-")
- 금액 3개 컬럼 정상 표시
- 분기 드롭다운 옵션 채워짐 확인

### Q7. 정의 목록 PASS
- 정의 탭 진입 시 마일스톤 행 표시 (이전: "등록된 마일스톤 없음")
- 개수 표시 정합

### Q8. 정의 등록·수정 모달 PASS
- 신규 등록 모달 → 4개 필수 키 정합 → 저장 PASS
- 기존 정의 수정 모달 → 기존 값 표시 PASS
- threshold 필드 ON/OFF 동작 확인

### Q9. EVENT_RANGE 범위 UI PASS
- 매출 검증 탭에서 EVENT_RANGE 카테고리 행 진입
- "범위: X~Y만원" 안내 + 금액 input 표시
- 금액 입력 후 승인 → bonus_formula 범위 검증 통과·반영

### Q10. 분기 드롭다운 PASS
- 결산 화면 상단 분기 드롭다운 옵션 채워짐
- 선택 시 결산 목록 필터링 동작

---

## §10 영역 라벨·자율주행

- A 트리거 헤더: **[R29-GAP-P1-A 근태 어드민 (프론트 주 + 백엔드 보조 2개)]**
- B 트리거 헤더: **[R29-GAP-P1-B 성과 어드민 (프론트 주 + 백엔드 보조 1개)]**
- C 트리거 헤더: **[R29-GAP-P1-C 라이브 검증]**

A·B 자율주행 정책 (`.claude/settings.json` + 트리거 본문 박음, CLAUDE.md §6.17):
- **묻기**: git push·설계 결정·package.json 수정·lib/auth.ts·admin-guard.ts·hyosung-parser.ts 수정
- **자율**: Read·Edit·Write·git add/commit/rebase·node --check·bash
- **금지**: force push·hard reset·rm -rf·금지 파일 수정

---

## §11 갱신 이력

| 시각 | 변경 |
|---|---|
| 2026-05-19 | 최초 작성 — P1 H 10건·P2 M 8건·L 10건 인벤토리 |
