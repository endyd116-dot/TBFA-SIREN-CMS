# 📋 SIREN 프로젝트 — 초정밀 인수인계 문서 v17 (통합 최종본)

> 📌 **이 문서 하나로 모든 세션(v1\~v16 + 세션 10) 작업 내역 + 규칙 + 정책 + 현재 미해결 이슈를 통합 파악**새 대화창에 본 문서 1개만 붙여넣으면 새 AI가 즉시 작업 가능

***

## 📌 1. 프로젝트 개요

| 항목      | 내용                                                   |
| ------- | ---------------------------------------------------- |
| 프로젝트명   | **SIREN (싸이렌)** — 교사유가족협의회 통합 NPO 플랫폼                |
| 운영 주체   | (사)교사유가족협의회 (사업자번호: 1188271215)                      |
| 사이트 URL | `https://tbfa-siren-cms.netlify.app`                 |
| 로컬 경로   | `C:\Users\Administrator\Desktop\작업\dev\tbfa-mis`     |
| 호스팅     | Netlify Pro (\$20/월) + Functions + Blobs + Scheduled |
| DB      | Neon PostgreSQL (Drizzle ORM, postgres-js)           |
| 저장소     | Cloudflare R2 (Pre-signed URL + base64 인라인)          |
| 결제      | 토스페이먼츠 + 효성 CMS+ + 토스 빌링 자동 청구                       |
| 이메일     | Resend (redirect 모드)                                 |
| AI      | Google Gemini 3.0-flash                              |
| 완성도     | **약 99.999%** (마스터플랜 14% / 3.04/22 Phase)            |
| 누적 작업   | **약 366h+**                                          |
| 도메인 보유  | `tbfa.co.kr` (메인 예정) + `yoonsiren.com` (리다이렉트 예정)    |

***

## 🛠 2. 기술 스택

| 영역                | 기술                                                |
| ----------------- | ------------------------------------------------- |
| Frontend          | Vanilla HTML/CSS/JS                               |
| Editor            | Toast UI Editor v3.2.2 (3-tier CDN fallback)      |
| Backend           | Netlify Functions v2 (Node.js 20)                 |
| Database          | Neon PostgreSQL + Drizzle ORM + postgres-js       |
| Auth              | JWT (사용자/관리자 분리) + bcryptjs + DUMMY\_BCRYPT\_HASH |
| AI                | Google Gemini 3-flash (모델별 폴백 chain)              |
| Email             | Resend (redirect 모드)                              |
| Storage           | Cloudflare R2 (Pre-signed URL + base64 인라인)       |
| **PDF (활동보고서)**   | **브라우저 인쇄(window\.print) + print CSS**            |
| PDF (영수증)         | pdf-lib + @pdf-lib/fontkit (NotoSansKR 6MB)       |
| Charts            | Chart.js 4.4 (report.html에서는 제거)                  |
| Cron              | Netlify Scheduled Functions                       |
| **Phase 1**       | **효성 CMS+ 엑셀 import (SheetJS)**                   |
| **Phase 2**       | **토스 빌링키 자동 청구 (cron)**                           |
| **Phase 3**       | **워크스페이스 (자체 캘린더 + 5 API)**                       |
| **Phase 3-extra** | **파일함 (R2 + 폴더트리 + ZIP + 휴지통 cron)**              |
| **ZIP**           | **JSZip 3.10.1 (CDN, 3-tier fallback)**           |

***

## 📁 3. 폴더 구조 (세션 10 종료 시점, \~321 파일)

```
tbfa-mis/
├── .env, .env.example, .gitignore
├── README.md
│
├── public/ (40 HTML + 46 JS + 23 CSS)
│   ├── _headers (X-Frame-Options: SAMEORIGIN)
│   │
│   ├── HTML 페이지 (40개)
│   │   ├── index.html
│   │   ├── admin.html (Phase 3: 사이드바에 워크스페이스 메뉴)
│   │   ├── admin-site-builder.html, admin-hub.html
│   │   ├── about.html, support.html, activities.html, activity.html
│   │   ├── news.html, report.html
│   │   ├── mypage.html (cancelGuideModal 포함)
│   │   ├── incidents.html, incident.html
│   │   ├── report-harassment.html, legal-support.html
│   │   ├── board.html, board-view.html, board-write.html
│   │   ├── campaigns.html, campaign.html
│   │   ├── resources.html
│   │   ├── password-reset.html, email-verify.html
│   │   ├── payment-success.html, payment-fail.html
│   │   ├── billing-register.html, billing-success.html, billing-fail.html
│   │   ├── 404.html, 500.html, privacy.html, terms.html
│   │   ├── ethics.html, email-reject.html
│   │   ├── cms-on.html, cms-si.html, cms-tbfa.html (Phase 1+2+3)
│   │   ├── workspace.html (Phase 3 Step 3)
│   │   ├── workspace-files.html (★ Phase 3-extra Step 5 — 290줄)
│   │   └── test-attachment.html (운영 전 삭제)
│   │
│   ├── partials/ (3개)
│   │   ├── header.html, footer.html, modals.html
│   │
│   ├── js/ (46개)
│   │   ├── common.js, home.js, charts.js
│   │   ├── admin-site-builder.js
│   │   ├── admin-home-hero.js, -quickmenu.js, -sections.js, -banner.js, -effects.js
│   │   ├── admin-mypage-cancellation.js
│   │   ├── admin.js (Phase 3 Step 3 — 5375 line 로그인 성공 시 /workspace.html)
│   │   ├── admin-chat.js, admin-hub.js, chat-user.js
│   │   ├── auth.js, donate.js, support.js
│   │   ├── news.js, editor.js, attachment.js, notification-bell.js
│   │   ├── incident.js, incident-detail.js
│   │   ├── harassment.js, legal.js, board.js
│   │   ├── mypage-applications.js
│   │   ├── admin-siren.js, admin-content.js
│   │   ├── about.js, activity.js, news-media.js
│   │   ├── admin-signup-sources.js, admin-campaigns.js, home-campaigns.js
│   │   ├── admin-activity-report.js
│   │   ├── admin-activity-report-list.js
│   │   ├── admin-resources.js
│   │   ├── admin-stats-edit.js, admin-approvals.js
│   │   ├── admin-incidents-crud.js
│   │   ├── cms-on.js, cms-si.js, cms-tbfa.js (Phase 1+2 — ~1700줄)
│   │   ├── workspace.js (블록 14 본격 구현 — 563줄)
│   │   └── workspace-files.js (★ Phase 3-extra Step 6~8 — 1,398줄)
│   │
│   └── css/ (23개)
│       ├── base.css, layout.css, components.css, pages.css, home.css
│       ├── admin.css, admin-hub.css, admin-site-builder.css
│       ├── cms-common.css, cms-on.css, cms-si.css, cms-tbfa.css
│       ├── editor.css, attachment.css, notification.css
│       ├── incident.css, harassment.css, legal.css
│       ├── board.css, activity.css
│       ├── print.css
│       ├── workspace.css (Phase 3 Step 3)
│       └── workspace-files.css (★ Phase 3-extra Step 5~8 — 878줄)
│
├── db/ (2개)
│   ├── index.ts
│   └── schema.ts (★ 56 테이블, ~1,743줄)
│
├── lib/ (32개)
│   ├── auth.ts, admin-guard.ts
│   ├── ai-gemini.ts, ai-attachments.ts
│   ├── ai-priority.ts, ai-incident.ts, ai-harassment.ts, ai-legal.ts, ai-reply.ts
│   ├── audit.ts, validation.ts, response.ts
│   ├── email.ts
│   ├── pdf-receipt.ts (영수증 — 유지)
│   ├── receipt-number.ts, notify.ts
│   ├── r2-client.ts, r2-server.ts
│   ├── r2-delete.ts (★ Phase 3-extra Step 8 — 25줄)
│   ├── csv-export.ts, member-classifier.ts
│   ├── grade-calculator.ts, churn-predictor.ts
│   ├── anniversary-checker.ts
│   ├── report-data-collector.ts, ai-report-generator.ts
│   ├── pdf-activity-report.ts (if(false)로 비활성화)
│   ├── donor-check.ts, site-settings.ts
│   ├── hyosung-parser.ts (Phase 1)
│   ├── toss-billing.ts (Phase 2)
│   └── workspace-logger.ts (Phase 3 — 284줄)
│
├── netlify/functions/ (★ 165개)
│   ├── [인증] auth-login, auth-register, auth-logout, ...
│   ├── [관리자 코어] admin-login, admin-stats, ...
│   ├── [회원] admin-members, admin-member-detail, ...
│   ├── [후원/결제] 14개
│   ├── [효성] admin-hyosung-import-contracts/billings (Phase 1)
│   ├── [지원/사이렌] 26개
│   ├── [어드민 사이렌] 9개
│   ├── [콘텐츠] 8개, [채팅] 6개, [AI] 6개
│   ├── [영수증] 2개, [알림] 2개, [Blob/R2] 4개
│   ├── [등급/이탈] 5개, [캠페인] 5개
│   ├── [활동보고서] admin-activity-report-ai.ts
│   ├── [기념일] 2개, [자료실] admin-resources.ts
│   ├── [Phase A] public-stats, admin-site-settings
│   ├── [Phase B] 메뉴/관련사이트 4개
│   ├── public-home-content, public-mypage-content
│   ├── public-activity-reports
│   ├── me-donor-status, admin-signup-sources
│   ├── [토스 빌링] admin-billing-keys, admin-billing-logs (Phase 2)
│   ├── [워크스페이스] admin-workspace-tasks/-memos/-events,
│   │                  admin-task-due-changes, admin-daily-briefing (Phase 3)
│   ├── [워크스페이스 부가] admin-workspace-member-list (Phase 3-extra Step 6)
│   ├── ★ [파일함 6 + 2 = 8개] (Phase 3-extra)
│   │   ├── admin-workspace-folders.ts (456줄)
│   │   ├── admin-workspace-files.ts
│   │   ├── admin-workspace-file-presign.ts
│   │   ├── admin-workspace-file-confirm.ts
│   │   ├── admin-workspace-file-download.ts
│   │   ├── admin-workspace-file-share.ts
│   │   ├── admin-workspace-file-purge.ts (★ Step 8 신규)
│   │   └── admin-workspace-folder-purge.ts (★ Step 8 신규)
│   └── [Cron] 9 + 1 = 10개
│       └── cron-workspace-trash-cleanup.ts (★ Step 8 신규, KST 03:00)
│
├── assets/fonts/NotoSansKR-Regular.ttf (6MB) — 영수증용
├── netlify.toml, package.json, package-lock.json
├── drizzle.config.ts, tsconfig.json
└── (인수인계서 v17 본 문서)
```

***

## 🗄 4. DB 스키마 — 56 테이블 (schema.ts \~1,743줄)

### 핵심 테이블 그룹

```
[기본] members, donations, billing_keys, audit_logs, sessions
[지원/사이렌] support_requests, incident_reports, harassment_reports, 
              legal_consultations, board_posts, board_comments
[등급/캠페인] member_grades, campaigns, churn_predictions
[콘텐츠] activities, news, faq, resources, resource_categories,
         site_settings, nav_menu_items
[채팅] chat_rooms, chat_messages
[알림] notifications
[영수증] receipts
[자동화] anniversary_logs, signup_sources

★ Phase 1 (효성):
  hyosung_contracts, hyosung_billings

★ Phase 2 (토스 빌링):
  billing_logs, card_expiry_alerts
  + members 4컬럼 + billingKeys.cardExpiryMonth + donations.billingLogId

★ Phase 3 (워크스페이스 — 7 테이블):
  workspace_tasks, workspace_memos, workspace_events,
  task_due_change_requests, daily_briefings,
  workspace_notifications, workspace_activity_log

★ Phase 3-extra (파일함 — 3 테이블, 세션 9 신규):
  workspace_folders (11 컬럼 + 4 인덱스)
  workspace_files (17 컬럼 + 5 인덱스)  ← sizeBytes: bigint
  workspace_file_shares (8 컬럼 + 3 인덱스)
```

### workspace\_folders 컬럼 상세 (세션 10 검증)

```typescript
export const workspaceFolders = pgTable("workspace_folders", {
  id: serial("id").primaryKey(),
  parentId: integer("parent_id"),
  name: varchar("name", { length: 200 }).notNull(),
  ownerId: integer("owner_id").notNull(),
  path: varchar("path", { length: 500 }),
  depth: integer("depth").default(0).notNull(),
  isShared: boolean("is_shared").default(false).notNull(),
  description: text("description"),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
  deletedAt: timestamp("deleted_at"),
}, (table) => ({
  parentNameUnique: uniqueIndex("ws_folders_parent_name_unique")
    .on(table.parentId, table.name)
    .where(sql`deleted_at IS NULL`),
  ownerIdx: index("ws_folders_owner_idx").on(table.ownerId),
  pathIdx: index("ws_folders_path_idx").on(table.path),
  deletedIdx: index("ws_folders_deleted_idx").on(table.deletedAt),
}));
```

### workspace\_files 컬럼 상세

```typescript
export const workspaceFiles = pgTable("workspace_files", {
  id: serial("id").primaryKey(),
  folderId: integer("folder_id"),
  ownerId: integer("owner_id").notNull(),
  name: varchar("name", { length: 300 }).notNull(),
  r2Key: varchar("r2_key", { length: 500 }).notNull(),
  sizeBytes: bigint("size_bytes", { mode: "number" }),
  mimeType: varchar("mime_type", { length: 100 }),
  ext: varchar("ext", { length: 20 }),
  sha256: varchar("sha256", { length: 64 }),
  uploadStatus: varchar("upload_status", { length: 20 }).default("pending").notNull(),
  downloadCount: integer("download_count").default(0).notNull(),
  description: text("description"),
  tags: jsonb("tags").default([]).notNull(),
  isShared: boolean("is_shared").default(false).notNull(),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
  deletedAt: timestamp("deleted_at"),
}, ...)
```

### schema.ts import 주의 (세션 9 발견)

```typescript
import {
  pgTable, serial, varchar, integer, text, timestamp,
  boolean, index, uniqueIndex, pgEnum, jsonb, bigint  // ★ bigint 필수
} from "drizzle-orm/pg-core";
```

***

## 🔐 5. 환경변수 (Netlify) — 변경 없음

```bash
# DB / JWT
NETLIFY_DATABASE_URL          # Neon PostgreSQL
JWT_SECRET, JWT_EXPIRES_IN=7d
ADMIN_JWT_SECRET, ADMIN_JWT_EXPIRES_IN=2h  # ★ 2h 만료
BCRYPT_ROUNDS=10

# 이메일 (Resend)
RESEND_API_KEY
RESEND_TEST_RECIPIENT=endyd116@gmail.com
EMAIL_FROM
SITE_URL=https://tbfa-siren-cms.netlify.app
ADMIN_NOTIFY_EMAIL

# 토스페이먼츠
TOSS_TEST_CLIENT_KEY, TOSS_TEST_SECRET_KEY
TOSS_MODE=test (라이브 시 'live')

# AI
GEMINI_API_KEY
GEMINI_MODEL_PRO=gemini-3-flash
GEMINI_MODEL_FLASH=gemini-3-flash

# Cloudflare R2
R2_ACCOUNT_ID, R2_BUCKET=siren-uploads
R2_ENDPOINT, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY

# 협회 정보
ORG_NAME, ORG_REGISTRATION_NO, ORG_REPRESENTATIVE
ORG_ADDRESS, ORG_PHONE
```

***

## 🚀 6. 세션 10 작업 내역 (2026.05.08, \~7h)

### ✅ 완료된 주요 작업

#### 1. 파일함 Step 5 — HTML + CSS 골격 (1.5h)

* `workspace-files.html` (\~290줄) 신규
* `workspace-files.css` (\~470줄) 신규
* `workspace-files.js` placeholder (\~25줄)
* 결정: Q1=A 독립 페이지 + Q2=A 독립 브레드크럼 + Q3=A Step 9에서 사이드바
* (옵션 D: Step 9에서 워크스페이스 사이드 패널 추가 예정)

#### 2. 파일함 Step 6 — workspace-files.js 본격 구현 (1.5h)

* placeholder → 본격 구현 (\~925줄)
* 신규 API: `admin-workspace-member-list.ts` (52줄)
* 모듈 13개: 인증/API/상태/트리/리스트/브레드크럼/뷰전환/업로드/CRUD/공유/검색/모달/토스트
* **마무리 패치**: API 응답 파싱 방어 6건 (data?.items → data?.data → 배열 fallback)

#### 3. 파일함 Step 7 — 검색+ZIP+공유+우클릭 (\~2h)

* `workspace-files.js` 925 → 1,268줄 (+343줄)
* `workspace-files.css` +80줄 (우클릭/ZIP/검색 스타일)
* 신규 기능:

  * ZIP 일괄 다운로드 (JSZip 3-tier CDN fallback)
  * 우클릭 컨텍스트 메뉴 (폴더 5개/파일 6개 항목)
  * 이동 다이얼로그 (다른 폴더로 이동)
  * 공유 권한 변경 (조회 ↔ 편집 드롭다운)
  * 키보드 단축키 (Ctrl+A, Delete, ESC)

#### 4. 파일함 Step 8 — 휴지통 + 영구삭제 + cron (\~2h)

**Block 1: 백엔드 신규 4개 파일**

* `lib/r2-delete.ts` (25줄) — R2 객체 삭제 유틸
* `admin-workspace-file-purge.ts` (90줄)
* `admin-workspace-folder-purge.ts` (130줄)
* `cron-workspace-trash-cleanup.ts` (130줄, KST 03:00)

**Block 2: 프론트 패치 9건 (Python)**

* `workspace-files.js` 1,268 → 1,398줄 (+130줄)
* `workspace-files.css` +40줄
* 휴지통 안내 배너 (노란색 강조)
* D-day 카운터 (D-30 초록 / D-7 노랑 / D-2 빨강)
* 강화된 confirm 메시지 ("30일 후 자동 영구 삭제")
* 영구삭제 모달 4개 체크리스트
* 일괄 복원 + 일괄 영구삭제 버튼

#### 5. 🔴 세션 10 발견된 핵심 버그 — 일부 미해결

**버그 1: `auth.response` → `auth.res` (해결됨)**

* `lib/admin-guard.ts`는 `{ ok, res }` 반환
* 9개 함수가 `auth.response`로 잘못 호출 → undefined → Netlify 자동 204
* 일괄 패치로 9개 파일 9건 수정 완료

**버그 2: 업로드 필드명 `fileName` → `name` (해결됨)**

* 클라이언트는 `body.fileName` 보냄
* 서버는 `body.name` 기대 → 400 "name 필수"
* workspace-files.js 패치로 해결 + folderId 0 → null 처리

**버그 3: 🔴 미해결 — 클라이언트/서버 파라미터 불일치 다수**

| API                                    | 서버가 기대                       | 클라이언트가 보냄                 | 상태     |
| -------------------------------------- | ---------------------------- | ------------------------- | ------ |
| DELETE /api/admin-workspace-files      | `?id=N`                      | `?fileId=N`               | 🔴 미해결 |
| PATCH /api/admin-workspace-files       | `?id=N&action=...`           | body만                     | 🔴 미해결 |
| GET /api/admin-workspace-file-download | `?id=N`                      | `?fileId=N`               | 🔴 미해결 |
| GET /api/admin-workspace-file-share    | `?targetType=...&targetId=N` | `?fileId=N`/`?folderId=N` | 🔴 미해결 |
| DELETE /api/admin-workspace-file-share | `?id=N`                      | `?shareId=N`              | 🔴 미해결 |
| DELETE /api/admin-workspace-folders    | `?id=N`                      | `?folderId=N`             | 🔴 미해결 |
| PATCH /api/admin-workspace-folders     | `?id=N`                      | body만                     | 🔴 미해결 |

**해결 방향 결정**: **옵션 A — 클라이언트를 서버에 맞춤** (서버 수정 X, 회귀 위험 0)

***

## 📊 7. 누적 코드량 (세션 10 종료)

| 파일                              | 세션 9 → 세션 10 | 증분          |
| ------------------------------- | ------------ | ----------- |
| workspace-files.html            | 신규 290줄      | +290        |
| workspace-files.css             | 신규 878줄      | +878        |
| workspace-files.js              | 60 → 1,398줄  | +1,338      |
| admin-workspace-member-list.ts  | 신규 52줄       | +52         |
| lib/r2-delete.ts                | 신규 25줄       | +25         |
| admin-workspace-file-purge.ts   | 신규 90줄       | +90         |
| admin-workspace-folder-purge.ts | 신규 130줄      | +130        |
| cron-workspace-trash-cleanup.ts | 신규 130줄      | +130        |
| **세션 10 합계**                    | ​            | **+2,933줄** |

***

## 📐 8. 파일함 Phase 3-extra 진행 상황

```
✅ Step 1: DB 마이그레이션 (1h) — 완료 (세션 9)
✅ Step 2: 폴더 API CRUD (2h) — 완료 (세션 9)
✅ Step 3: 파일 API 기본 (2.5h) — 완료 (세션 9)
✅ Step 4: 파일 편집 + 공유 API (2h) — 완료 (세션 9)
✅ Step 5: HTML + CSS 골격 (1.5h) — 완료 (세션 10)
✅ Step 6: 핵심 JS (3h) — 완료 (세션 10)
✅ Step 7: 검색 + ZIP + 공유 (2h) — 완료 (세션 10)
✅ Step 8: 휴지통 + cron (2h) — 완료 (세션 10)
🔴 Step 8 후속: 클라이언트/서버 파라미터 통일 패치 (예상 30분)
⏸ Step 9: 통합 라우팅 + 사이드바 + 사이드 패널 (~2h)

진행률: 88% (8/9 Step + 패치 1건 미해결)
```

***

## 🚨 9. Critical 주의사항 — 세션 10 신규 + 갱신 (총 60건)

### 🔴 58. requireAdmin 반환 필드 `res` (세션 10 신규 — 매우 중요)

```typescript
// lib/admin-guard.ts (실제 시그니처)
export async function requireAdmin(req: Request): Promise<
  | { ok: true; ctx: AdminContext }
  | { ok: false; res: Response }      // ★ res (response 아님!)
> { ... }

// ❌ 잘못된 호출 (세션 9~10 전반에 걸쳐 발견됨)
if (!auth.ok) return auth.response;   // undefined → Netlify 자동 204

// ✅ 올바른 호출
if (!auth.ok) return auth.res;
```

**증상**: API가 Status 204 + Body 빈 문자열 반환 → 클라이언트는 성공으로 오인
**원인**: TypeScript 타입 추론 실패로 컴파일 에러 안 남
**파급**: Phase 3-extra 9개 함수 모두 영향
**검증법**: `grep -rn "auth.response" netlify/functions/`

### 🔴 59. 파일 업로드 필드명 통일 (세션 10 신규)

```javascript
// ❌ 클라이언트 (잘못됨)
body: { fileName: file.name, ... }

// ✅ 서버가 기대
const name = String(body.name || "").slice(0, 300).trim();

// 정답: 클라이언트가 body.name 사용
```

### 🔴 60. 클라이언트/서버 파라미터 일관성 검증 필수 (세션 10 신규)

**현재 SIREN 표준 (검증된 패턴)**:

* `admin-workspace-folders.ts`: `?id=N` (DELETE/PATCH)
* `admin-workspace-files.ts`: `?id=N` (DELETE/PATCH)
* `admin-workspace-file-download.ts`: `?id=N`
* `admin-workspace-file-share.ts`: `?targetType=folder|file&targetId=N` (GET/POST), `?id=N` (DELETE)
* `admin-workspace-file-purge.ts`: `?fileId=N` (Step 8 신규, 통일 안 됨)
* `admin-workspace-folder-purge.ts`: `?folderId=N` (Step 8 신규, 통일 안 됨)

**향후 신규 함수 작성 시**:

* 기존 함수 파라미터 패턴 grep으로 먼저 확인
* 표준 어긋나는 신규 도입 시 사용자 명시 승인 받기

### 🔴 55. httpOnly 쿠키 환경 + SPA 핸들러 (기존 v16)

### 🔴 56. /api/\* 경로는 `export const config` 필수 (기존 v16)

### 🔴 57. schema.ts에 bigint import 확인 (기존 v16)

### 🔴 1\~54. (v15 인수인계서 그대로 유지 — 너무 길어 생략, v15 참조)

***

## 📊 10. 22개 Phase 마스터 로드맵 (진행률 업데이트)

```
🔥 긴급 (운영 리스크)
├─ Phase 1: 효성 CMS+ 연동 ✅ (완료)
└─ Phase 2: 토스 빌링 자동 청구 ✅ (완료)

🏛 핵심 인프라
├─ Phase 3: 공통 워크스페이스 + Agent-8 🟡 (87%)
│   └─ Step 1~3 완료, Step 4~6 대기
│   └─ ⭐ Step 7 (협업 고도화: 칸반+보류+보관+코멘트+팀뷰, ~17~19h)
│      세션 10에서 사용자와 합의 완료 (3개 세션 분할)
├─ Phase 3-extra: 파일함 🟡 (88%, 8/9 Step + 미해결 1건)
│   └─ Step 1~8 완료, Step 9 + 파라미터 통일 패치 대기
└─ Phase 4: 대표 보고 시스템 + Agent-9

💰 재정 관리
├─ Phase 5~7

📨 커뮤니케이션
├─ Phase 8~11

🚨 SIREN 서비스 고도화
├─ Phase 12~15

📊 분석·경영
├─ Phase 16~18

🛠 품질·안정성
├─ Phase 19~22

총 진행률: 14% (3.04/22 Phase + 파일함 0.88)
누적 시간: 366h+
```

***

## 🎯 11. Phase 3 Step 7 설계 — 사용자 합의 완료 (세션 10)

### 📐 칸반 5컬럼 구조

```
[📋 준비중] todo → [🔄 업무중] in_progress ↔ [⏸ 보류] on_hold → [✅ 완료] done → [📦 보관] archived
```

### 신규 테이블 4종 (Step 7 마이그레이션)

```sql
workspace_task_comments (Q4 코멘트)
workspace_task_reports (중간/완료 보고)
workspace_task_attachments (카드↔파일 연결, 파일함 FK)
workspace_task_templates (반복 업무 템플릿)
```

### workspace\_tasks 확장 (Step 7)

```sql
ALTER TYPE task_status ADD VALUE 'on_hold';
ALTER TYPE task_status ADD VALUE 'archived';

ALTER TABLE workspace_tasks ADD COLUMN
  progress_percent integer DEFAULT 0,
  estimated_hours numeric(5,1),
  actual_hours numeric(5,1),
  hold_reason text,
  hold_started_at timestamp,
  archived_at timestamp,
  subtasks jsonb DEFAULT '[]',
  tags text[] DEFAULT ARRAY[]::text[],
  bookmarked_by jsonb DEFAULT '[]',
  ai_summary text,
  ai_risk_score integer,
  ai_risk_updated_at timestamp;
```

### 채택된 기능 6종

| 기능        | 구현 방식                          |
| --------- | ------------------------------ |
| 알림 센터 고도화 | workspace\_notifications 확장    |
| @멘션       | mentions jsonb + 즉시 알림         |
| 업무 템플릿    | workspace\_task\_templates 테이블 |
| 통합 검색     | PostgreSQL ILIKE               |
| 북마크       | bookmarkedBy jsonb             |
| 타임 트래킹    | actualHours 자동 계산              |

### AI 기능 3종 (Tier 1)

| 기능             | 트리거               | 동작                      |
| -------------- | ----------------- | ----------------------- |
| AI-1 요약        | 카드 생성 시           | 3줄 요약 → ai\_summary 캐시  |
| AI-2 리스크 예측    | 매일 KST 06:00 cron | 지연 확률 → ai\_risk\_score |
| AI-3 완료 보고서 초안 | done 이동 시         | 활동로그+체크리스트+댓글 분석        |

### 뷰 3종

```
[📊 칸반] 5컬럼 (todo/in_progress/on_hold/done/archived)
[📅 캘린더] 업무(tasks) + 일정(events) 통합
[📋 리스트] 기존 블록 14 뷰 (참고용 유지)
```

### 카드 상세 모달 — 7개 탭

```
[📝 개요] [💬 댓글] [📎 파일] [📊 보고]
[✅ 체크리스트] [📜 히스토리] [🤖 AI]
```

### Step 7 분할 (3 단위)

```
Step 7-A: 칸반 5컬럼 + 보류/보관 (5h)
Step 7-B: 카드 고도화 (댓글/파일/보고/체크리스트/북마크) (6h)
Step 7-C: 캘린더 + AI + 검색/템플릿/타임트래킹 (6~8h)
```

***

## 🐛 12. 미해결 이슈 (세션 10 종료 시점)

### 🔴 1순위 — 파일함 파라미터 통일 (즉시, 30분)

**증상**: 파일/폴더의 삭제, 이름변경, 다운로드, 공유, 복원 모두 작동 안 함
**원인**: 클라이언트와 서버의 query parameter 명명 불일치
**해결 방향**: 클라이언트 측을 서버 표준에 맞춤 (위 Critical #60 참조)
**영향 범위**: workspace-files.js 7개 함수

```
✅ 작동: 폴더 생성, 파일 업로드, 데이터 조회
🔴 미작동: 파일 삭제, 폴더 삭제, 이름 변경, 공유 추가, 다운로드,
          영구 삭제, 복원, 이동
```

### 🟡 2순위 — Phase 3-extra Step 9 (\~2h)

```
- admin.html / cms-tbfa.html 사이드바에 [📁 파일함] 메뉴 추가 (Critical #47, #48)
- workspace.html 우측 사이드 패널 (Q1-D 옵션 — 메모/작업 보면서 파일 관리)
- 통합 테스트
```

### 🟠 3순위 — Phase 3 본체 마무리 (5.5h)

```
⏸ Step 5 Agent-8 스케줄러 cron (~3h)
⏸ Step 6 통합 라우팅 + 테스트 (~2.5h)
⏸ Step 7 협업 고도화 (~17~19h, 3개 세션)
   ※ Step 4 모달 CRUD는 스킵 (Step 7-B로 대체)
```

### 🔴 4순위 — 자잘한 버그 3건 (3h)

```
🟢 14번 — 시스템 설정 정기/일시 후원 금액 표시 딜레이 (30분)
🟢 1번 — 회원관리 엑셀 추출 (1h)
🟢 2번 — 국세청 엑셀 / 영수증 일괄 발행 (2h)
```

### 🟠 5순위 — 중간 작업 4건 (10h)

```
🟠 4번 — 채팅 블랙 관리 탭 신설 (3h)
🟠 5번 — 회원 정지 vs 블랙 정리 (1h)
🟠 9번 — Phase B Step 5-B/C 메뉴 편집 UI + 푸터 (5h)
🟠 10번 — 정기후원 해지 안내 CRUD 추가 편집 (1h)
```

### 🔴 6순위 — 큰 기능 3건 (38\~50h)

```
🔴 6번 — 교원 회원 자격 변경 시스템 (8~10h)
🔴 8번 — 변호사/심리상담사 ↔ 사용자 1:1 매칭 채팅 (10~15h)
🔴 15번 — 효성 + 기업은행 CSV 자동 매핑 + 후원 확정 워크플로 (10~15h)
```

***

## 💬 13. 코딩 규칙 — 절대 준수 (세션 10 갱신)

### A. 작업 흐름 (변경 없음)

1. **요구사항 → 설계 → 사용자 승인 → 코드 → 검증** 5단계
2. 매 STEP 끝에 `git push` 명령어 제공
3. 마이그레이션은 1회용 → 호출 → 즉시 삭제 (보안)
4. 2\~3 step씩 묶어서 사용자 OK 받고 다음 진행
5. 무한루프 금지 — 사용자와 대화하며 step-by-step

### B. 코드 전달 방식 (세션 10 강화)

```
1. 1500줄 이하 코드 → 파일 전체 통째로 교체 (한 번에)
2. 1500줄 초과 + 단순 1~2 수정 → Python sed 패치
3. 1500줄 초과 + 광범위 수정 → 메서드 단위 분할
4. 한 메시지당 3000줄 코드 넘으면 즉시 멈추고 분할
5. ★ 답변 분할 시 → 두 Part 각각 완결된 Bash 블록 권장
   (Part 1+2 합치기 어려움 → 사용자 실수 유발)
6. 답변 마지막에 "따뜻한 마음" 같은 감성 표현 금지
```

### C. 코드 수정 안내 4요소 명시

```
✅ 무엇을 찾을지 — 검색 키워드 또는 함수명 명시
✅ 어떻게 바꿀지 — 교체/추가/삭제 명시
✅ 어디에 위치할지 — 파일 내 정확한 위치
✅ 왜 바꾸는지 — 1줄 이유

❌ 함수명 위주 X — 기능과 로직 위주로 설명
```

### D. 검증은 Phase 끝에 한 번에 (세션 10 강화)

```
❌ 매 단계마다 검증 요구 (매번 끊김)
✅ Phase 전체가 끝날 때 통합 체크리스트 제공
✅ ★ POST/PATCH/DELETE는 GET 검증과 별개로 실측 필수
   (세션 10 교훈: GET만 검증하면 백엔드 버그 누적)
```

### E. 진단 반전 방지 원칙 (세션 10 강조)

```
✅ F12 콘솔/네트워크 실데이터 먼저 요청
✅ 가설 1~2개만 제시 + 증거 기반으로 좁힘
✅ 추측 패치 금지
✅ 진단 1회 반전 시 사용자에게 "차분히" 제안
✅ 진단 2회 이상 반전 시 즉시 세션 종료 권장
✅ ★ 코드 직접 보지 않고 추측 금지 (세션 10 교훈)
   "검토 필요한 코드 통으로 줘" 요청이 가장 빠른 방법
```

### F. Bash 통합 블록 (세션 4\~5 확정, 변경 없음)

### G. 커밋 메시지 규칙 (변경 없음)

### H. 마이그레이션 파일 처리 (변경 없음)

### I. Python 자동 패치 패턴 (변경 없음)

### J. 백업 전략 (변경 없음)

### K. 답변 가이드 (세션 10 강화)

1. 모든 질문에 추천안 (⭐) + 2\~3개 대안 제시
2. 결정 사항은 한글로 풀어서 설명
3. 컨텍스트 누적 시 새 세션 권장 (90% 도달 시 1회만 안내)
4. 단언 금지 — "무조건 될 것", "100% 작동"
5. 반응성 멈춤 시 그 부분부터 정확히 이어서 진행
6. 사용자가 폭주 지적하면 즉시 멈추고 정리
7. ★ **새 함수 작성 전 기존 패턴 grep 필수** (세션 10 신규)

***

## 🎯 14. 새 대화 시작 방법 (다음 AI에게)

### 1단계: 본 v17 문서 통째로 붙여넣기

### 2단계: 첫 메시지 (예시)

```
SIREN v17 통합 인수인계서 받음.

[현재 상태]
✅ Phase 1, 2 완료
🟡 Phase 3 (워크스페이스) 87%
🟡 Phase 3-extra (파일함) 88% — Step 1~8 완료
   🔴 미해결: 클라이언트/서버 파라미터 불일치 7개 API
   ⏸ Step 9 대기

[다음 작업: Phase 3-extra Step 8 후속 패치]
파일함의 다음 액션이 모두 작동 안 함:
- 파일 삭제, 폴더 삭제
- 이름 변경 (파일/폴더)
- 다운로드
- 공유 추가/제거/권한변경
- 영구 삭제
- 복원
- 이동

서버 표준 파라미터:
- DELETE/PATCH /api/admin-workspace-files: ?id=N
- DELETE/PATCH /api/admin-workspace-folders: ?id=N
- GET /api/admin-workspace-file-download: ?id=N
- GET/POST /api/admin-workspace-file-share: ?targetType=&targetId=N
- DELETE /api/admin-workspace-file-share: ?id=N
- /api/admin-workspace-file-purge: ?fileId=N (예외)
- /api/admin-workspace-folder-purge: ?folderId=N (예외)

클라이언트 workspace-files.js를 위 표준에 맞춰 일괄 패치 필요.

[규칙 유지]
- Bash 통합 블록 (작업+검증+push)
- 1500줄 이하 통째 교체
- 옵션 A~C 제시 후 승인
- echo 안 git 명령어 X, 커밋 메시지 ! X
- 단언 X, 감성 표현 X
- 진단 반전 2회 시 세션 종료
- 컨텍스트 90% 도달 시 1회만 안내
- httpOnly 쿠키 환경 — 클라이언트 토큰 체
```

​
