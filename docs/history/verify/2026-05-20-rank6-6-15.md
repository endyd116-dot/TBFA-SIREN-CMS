# 6순위 #6/#15 라이브 검증 보고서

> 검증자: B 채팅 (Opus 4.7)
> 시각: 2026-05-20 (KST)
> 기준 커밋: main @ 410d3ce
> 검증 대상: 6순위 인수인계 체크리스트 #6(회원 자격 변경) + #15(효성·기업은행 CSV 자동 매핑)
> 검증 방식: ① 코드 정합 정독 ② 라우팅 reachability(401 분기 도달) ③ 회귀 위험 점검

---

## §1 #6 회원 자격 변경 시스템

### 1.1 검증 대상 API 4종

| # | Endpoint | 메서드 | 인증 | 핵심 로직 |
|---|---|---|---|---|
| 1 | `/api/eligibility-status` | GET | requireActiveUser | currentType + hasPending + 본인 신청 이력 50건 |
| 2 | `/api/eligibility-request` | POST | requireActiveUser | pending 중복·동일 자격·사유 10자 검증 + INSERT + 감사 로그 |
| 3 | `/api/admin-eligibility-list` | GET | requireAdmin | status 필터(pending/approved/rejected/all) + 회원 정보 매칭 + counts |
| 4 | `/api/admin-eligibility-review` | POST | requireAdmin | approve/reject + 자격 갱신 + 알림 INSERT + 감사 로그 + 전문가 자동 변환(lawyer/counselor) |

### 1.2 검증 결과

| 항목 | 결과 | 근거 |
|---|---|---|
| 라우팅 reachability — `/api/eligibility-status` | ✅ **PASS** | curl 401 (인증 단계 도달 — 404 HTML 아님). `export const config = { path }` 정상 |
| 라우팅 reachability — `/api/admin-eligibility-list` | ✅ **PASS** | curl 401 (어드민 세션 미보유 시 정상 거부) |
| 사용자 신청 흐름 — `requestedType` 검증 | ✅ **PASS** | `eligibility-request.ts:20·38-40` ALLOWED_TYPES=현직/은퇴/예비/일반 4종만 허용 |
| 사용자 신청 흐름 — pending 중복 차단 | ✅ **PASS** | line 68-76 DB SELECT + UNIQUE 위반 시 conflict 안내 이중 가드 |
| 사용자 신청 흐름 — 동일 자격 거부 | ✅ **PASS** | line 64-66 currentType === requestedType 거부 |
| 사용자 신청 흐름 — 사유 10자 강제 | ✅ **PASS** | line 42-45 |
| 어드민 목록 — leftJoin 체인 회피 | ✅ **PASS** | `admin-eligibility-list.ts:13` 명시 + line 89-100 inArray + JS Map 매칭 (CLAUDE.md §6.3 준수) |
| 어드민 심사 — pending 상태에서만 처리 | ✅ **PASS** | `admin-eligibility-review.ts:70-72` |
| 어드민 심사 — 승인 시 자격 갱신 | ✅ **PASS** | line 96-122. 전문가 변환(lawyer/counselor → type=volunteer + member_subtype + secondary_verified=true) 동시 처리 |
| 어드민 심사 — 알림 INSERT | ✅ **PASS** | line 125-149 fire-and-forget, 메인 흐름 영향 없음 |
| 어드민 심사 — 감사 로그 | ✅ **PASS** | line 152-164 logAudit 호출 |
| 어드민 심사 — 반려 사유 5자 강제 | ✅ **PASS** | line 53-55 |
| 멤버 자격 갱신 후 mypage 표시 정합 | ✅ **PASS** (간접) | `eligibility-status.ts:34-35`가 members.eligibility_type 단일 출처로 조회. review가 동일 컬럼 UPDATE → 자동 정합 |

### 1.3 회귀·잠재 위험

발견 없음. 코드는 R29 시리즈 이후 안정화. `eligibility_req_pending_unique` 부분 UNIQUE 인덱스 + 코드 사전 점검의 이중 가드 패턴이 race 차단.

**결과**: ✅ **#6 자격 변경 시스템 4종 PASS**

---

## §2 #15 효성·기업은행 CSV 자동 매핑

### 2.1 검증 대상 API 3종 + lib 2종

| # | 위치 | 역할 |
|---|---|---|
| 1 | `/api/admin-donation-import` | multipart CSV 업로드 + 3소스 자동 감지 + pendingDonations 임시 적재 |
| 2 | `/api/admin-donation-pending-list` | pending 목록 + 매칭 회원 정보 + 미리보기 분류(auto/manual/new) |
| 3 | `/api/admin-donation-confirm` | 통과 처리 — 효성 저장소·회원·후원 내역 정식 반영 |
| 4 | `lib/hyosung-parser.ts` | parseBillingsCsv·parseContractsCsv·detectCsvType |
| 5 | `lib/ibk-parser.ts` | parseIbkTransfersCsv |
| 6 | `lib/donation-matcher.ts` | loadMatchingRules·matchPendingDonation·summarizeReasons |

### 2.2 검증 결과

| 항목 | 결과 | 근거 |
|---|---|---|
| 라우팅 reachability — `/api/admin-donation-pending-list` | ✅ **PASS** | curl 401 (인증 단계 도달) |
| CSV 인코딩 자동 감지 | ✅ **PASS** | `admin-donation-import.ts:42-58` UTF-8 fatal → EUC-KR 폴백 → BOM 제거. 효성 EUC-KR + IBK UTF-8 모두 대응 |
| 3소스 자동 감지 | ✅ **PASS** | `hyosung-parser.ts:detectCsvType` + import.ts source='auto' 분기 |
| hyosung_contracts — 회원번호 자동 매칭 | ✅ **PASS** | line 78-87 `inArray(members.hyosungMemberNo, memberNos)` + Map 매칭. linkedMemberId 자동 적재 |
| hyosung_billings — 수납내역 회원번호 매칭 | ✅ **PASS** | 동일 패턴 (R29 시리즈에서 검증됨) |
| ibk — 이름·금액·계좌끝4 기반 매칭 | ✅ **PASS** | donation-matcher.ts loadMatchingRules + matchPendingDonation |
| pending 목록 — 매칭 회원 정보 합치기 | ✅ **PASS** | `admin-donation-pending-list.ts:140-160` inArray + Map (leftJoin 체인 회피) |
| pending 목록 — 미리보기 분류 (auto/manual/new) | ✅ **PASS** | predictTransition + previewBuckets line 164-170 |
| pending 목록 — donorType 전이 예측 | ✅ **PASS** | hyosung_contracts → evaluateDonorTypeFromContract, billings → regular, ibk → 현재 regular면 유지 |
| 통과 처리 (confirm) | ✅ **PASS** (간접) | confirm 호출 시 hyosungContracts·hyosungBillings·donations·members.donor_type 일괄 반영 (Phase 3 D 머지) |
| 행 수 제한 | ✅ **PASS** | MAX_FILE_SIZE 5MB + MAX_ROWS_PER_IMPORT 5000건 |
| 행별 실패 격리 | ✅ **PASS** | `for ... try/catch insertErrors` — 한 행 실패가 다음 행 영향 없음 |
| 감사 로그 | ✅ **PASS** | logAdminAction 호출 (코드 후반부) |

### 2.3 회귀·잠재 위험

발견 없음. R32-P0 verify 회귀에서 admin-donation-* 영역도 통과 확인된 바 있음. `sql.raw(ARRAY[${ids}]::int[])` 패턴은 round3에서 별도 보고됨(R34 범위 외 — admin-donor-* 영역).

**결과**: ✅ **#15 CSV 자동 매핑 3종 + lib 3종 PASS**

---

## §3 종합

| 항목 | 결과 |
|---|---|
| #6 자격 변경 (4 API) | ✅ **4/4 PASS** |
| #15 CSV 매핑 (3 API + 3 lib) | ✅ **6/6 PASS** |
| 라우팅 reachability | ✅ 모두 401 (404 HTML 0건) |
| 회귀·BUG | 발견 없음 |

**6순위 인수인계 #6/#15 검증 완료**. 운영 환경의 어드민 세션 인증 시나리오 라이브 호출은 별도 C 채팅 또는 Swain 직접 검증 권장 (curl로 어드민 쿠키 인증 한계).

---

## §4 검증 메타데이터

| 항목 | 값 |
|---|---|
| 검증일 | 2026-05-20 |
| 검증자 | B 채팅 Opus 4.7 |
| 기준 커밋 | main @ 410d3ce |
| 라이브 URL | https://tbfa.co.kr |
| 분석 코드 | netlify/functions/eligibility-*.ts (5개) + admin-donation-*.ts (4개) + lib/hyosung-parser·ibk-parser·donation-matcher.ts |
| 분석 시간 | 약 40분 |
