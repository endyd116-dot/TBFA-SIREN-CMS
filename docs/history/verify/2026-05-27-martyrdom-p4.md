# 딥릴리프 P4 검증 보고서 (C) — 2026-05-27

> 라운드: 딥릴리프(순직 인정 지원) P4 마감 — AI 비서 순직 읽기 도구 / 유족 전달용 쉬운 요약 / 인정률·성과 통계 / 연구 발간지
> 검증자: C (Opus 4.7) · 브랜치 `verify/martyrdom-p4` (베이스 로컬 main @ e62618d)
> 검증 분담(§7): C = 코드·라우팅·격리·tsc + 실서버 API 라이브 / Swain = 브라우저 UI
> 결과: **코드·외부화·격리 검증 통과 + 라이브에서 차단 결함 3건 발견·수정** (검증 브랜치 커밋, 메인 머지·배포 후 재검증 필요)

---

## 1. 한눈 요약

| 구분 | 결과 |
|---|---|
| 코드 정합(응답키·schema·라우팅·tsc·캐시버스터) | ✅ PASS |
| ★ 격리(Q7) — 일반 검색 `qna·manual` 불변·순직 도구 운영자 게이트·직접 조회 | ✅ PASS (코드 + 라이브) |
| ★ 외부화 스모크(L0) — 다양한 의존성 함수 모듈 로드 | ✅ PASS (모듈 오류 0건) |
| 회귀 — P1·P2·P3 | ✅ PASS (P4는 순수 추가·기존 함수 본문 미수정) |
| 라이브 기능(L1 유족요약·L3 발간 생성·L4 AI 도구) | ❌→🔧 **배포본에서 차단 결함 3건** → 검증 브랜치에서 수정 |
| 수정 건수 | **8건**(차단 3 + 품질 3 + 권한 2) — 전부 tsc 통과 |
| 권한 정책 | Swain 결정 2건 반영·수정 완료 (§5) |

배포본(현 main)으로 라이브를 돌려 **사용자가 실제로 쓰는 3개 기능이 막혀 있던 것**을 찾아냈습니다. 모두 검증 브랜치에 고쳤고, 메인이 머지·배포하면 동작합니다.

---

## 2. 코드 검증 (A1~A4) — PASS

### A1 응답키·schema·라우팅·tsc·캐시버스터
- **응답 키 §P4.2 계약 1:1 일치**: 백엔드가 내려주는 키와 화면이 읽는 키가 정확히 맞음 — 유족요약(`summary{contentText,nextSteps,status}`) / 통계(`totals·recognitionRate·byCaseType·byStatus·trend`) / 발간 생성(`queued·id`) / 발간 상세(`publication{contentHtml·reidRisk·blendRatio…}`) / 목록(`publications[]`) / 내보내기(`fileName·mimeType·base64`).
- **schema 정합**: 새 발간물 보관함(`martyrdom_publications`)이 schema에 정식 활성화됨(설계서 컬럼·기본값 그대로). 1회용 마이그 파일은 호출 후 삭제됨(확인).
- **라우팅**: 신규 5개 함수 모두 주소 경로(`export const config = { path }`) 정상 — 미인증 호출 시 전부 404 아닌 401(로그인 요구) 반환.
- **타입체크**: `npx tsc --noEmit` 통과(수정 후 재실행 포함).
- **구문**: `node --check public/js/admin-martyrdom.js` 통과.
- **캐시버스터**: 화면 스크립트 `admin-martyrdom.js?v=P4` 반영, `USE_P4_MOCK=false`(실 API 연결).

### A2 ★ 격리(Q7) — 최우선
- **일반 검색 격리 불변**: AI 비서의 일반 자료 검색은 여전히 일반 자료(`qna·manual`)만 본다 — P2에서 잠근 부분 한 글자도 안 바뀜.
- **순직 도구 운영자 게이트**: 신규 순직 조회 도구 3개(사건 목록·사건 상태·임박 기한)는 전부 운영자 이상만 통과(`ensureRole`). 슈퍼관리자는 자동 통과.
- **직접 조회**: 세 도구 모두 순직 전용 테이블을 **직접** 조회하고 일반 검색 엔진을 거치지 않음 → 순직 민감자료가 일반 검색으로 새지 않음.

### A3 응답키 1:1 (백 ↔ 화면 mock)
- 화면 소비 지점 전수 확인 — `fs.summary.contentText/nextSteps`, `d.totals/recognitionRate/byCaseType/byStatus/trend`, `d.queued/id`, `d.publication.contentHtml/reidRisk/blendRatio`, `d.publications`, `d.fileName/base64` — 모두 계약과 일치.

### A4 회귀 (P1·P2·P3)
- P4 백엔드 커밋은 **순수 추가**(새 테이블·새 함수·새 도구만, 기존 P1/P2/P3 함수 본문 미수정 — diff 전량 `+`). 전체 타입체크 통과로 컴파일 회귀 없음 확인.

---

## 3. 라이브 검증 (실서버 tbfa.co.kr · 슈퍼관리자 로그인)

> 라이브는 **현재 배포본(main @ e62618d, 수정 전)** 기준. 아래 결함은 배포본 결함이고, 수정은 검증 브랜치에 반영됨(배포 후 재검증 대상).

### ★ L0 외부화 스모크 — ✅ PASS (최우선)
이번 배포에 함수 번들 외부화가 적용됐는데, 다양한 의존성을 쓰는 함수들이 "모듈을 못 찾음" 류 오류 없이 정상 로드되는지 확인:
| 함수 | 의존 | 결과 |
|---|---|---|
| 순직 사건 목록 | DB 조회 엔진 | 모듈 로드 정상(401) |
| 순직 통계 집계 | 집계 쿼리 | 모듈 로드 정상(200·집계 동작) |
| 서면 PDF 내보내기 | PDF 생성기 | 모듈 로드 정상(401) |
| 발간물 PDF 내보내기 | PDF 생성기 + 한글폰트 | **PDF 실제 생성 성공(약 3MB·한글 임베드)** |
| RAG 상태 | 임베딩 | 모듈 로드 정상(401) |
→ "모듈 못 찾음" 500 **0건**. 외부화로 인한 깨짐 없음.

### L1 유족 전달용 요약 생성 — ❌ 배포본 차단 → 🔧 수정
- 사건(caseId=2)에 유족 요약 생성 요청 → **500 오류**. 원인: 저장 시 **존재하지 않는 컬럼(`updated_at`)에 쓰려 함** → 유족 요약 기능이 통째로 막힘.
- 수정: 저장 항목에서 없는 컬럼 제거(이 테이블은 생성시각만 보유).

### L2 통계 — ✅ PASS
- 인정률·총계·유형별·상태별·월별 추이 전부 정상 집계: 총 2건·인정 1건·인정률 0.5·월별 추이 1건.
- (사소) 유형 미지정 사건이 "미분류" 막대로 **두 개**로 갈라져 표시됨 → 집계 묶음 기준을 표시값과 일치시켜 하나로 합치도록 수정.

### L3 발간물 생성→검수→발간→내보내기 — ❌ 배포본 차단 → 🔧 수정
- 발간물 생성 요청 → 접수(`queued·id`) 정상. 그러나 **본문이 영영 만들어지지 않음**(2분 폴링 내내 빈 본문·draft).
- 원인: 본문 생성을 맡는 백그라운드 작업을 **잘못된 내부 주소(`/api/...`)로 호출** → 그 주소는 없어서 404 → 호출 실패가 조용히 삼켜짐. (프로젝트의 다른 모든 백그라운드 호출은 `/.netlify/functions/...`를 씀.)
- 수정: 내부 호출 주소를 올바른 경로로 변경.
- 본문 생성 외 단계는 정상 확인: **검수→발간 상태 전이 정상**(`reviewed`→`published`), **슈퍼관리자 게이트 통과**, **PDF/HTML 내보내기 응답 계약 정상**(PDF 3MB 생성). HTML 내보내기는 조립본(charset·면책 포함) 대신 본문 원본만 인코딩하던 문제도 함께 수정.

### L4 AI 비서 순직 도구 — ❌ 배포본 미발화 → 🔧 수정 (도구 자체는 정상)
- "진행 중 순직 사건 알려줘"류 질문에 순직 도구가 **안 불림**(엉뚱하게 알림 템플릿 목록을 답함).
- 원인: 순직 도구 3개가 AI 비서의 **의도별 도구 묶음(키워드 라우터)에 등록 안 됨** → "사건"이 들어간 질문은 SIREN 신고 묶음으로 가고 순직 도구는 후보에서 빠짐 → 모델이 순직 도구를 볼 수 없었음.
- **도구 자체는 정상 확인**: 도구를 후보에 넣어 물어보면(그룹 무매칭 질의) 모델이 `martyrdom_case_list`를 정확히 호출하고 사건(`MTR-20260526-0002 제주중 현승준 선생님·상태 접수·준비도 15%`)을 반환. 권한 2중 게이트(슈퍼관리자) 정상 통과.
- 수정: AI 비서 도구 라우터에 "순직" 묶음 추가(키워드 `순직·유족급여·딥릴리프·재해보상·공무상 사망`). 일반 검색 격리는 그대로(격리 불변).

### L5 권한 — ✅ PASS
- 쿠키(로그인) 없이 호출 → 전부 401.
- 발간물 상태 변경·삭제는 슈퍼관리자만 통과(현 슈퍼관리자 계정으로 정상 동작 확인).

### Q7 격리 라이브 — ✅ PASS
- 일반 질문("최근 후원 통계 요약")에 순직 도구 **미발화** 확인.

---

## 4. 수정 내역 (검증 브랜치 `verify/martyrdom-p4`)

| # | 성격 | 파일 | 내용 |
|---|---|---|---|
| 1 | **차단**(라이브) | `lib/martyrdom-ai.ts` | 유족요약 저장이 없는 컬럼(`updated_at`)에 쓰려다 500 → 제거 (L1 복구) |
| 2 | **차단**(라이브) | `netlify/functions/admin-martyrdom-publication.ts` | 발간 본문 백그라운드 호출 주소 `/api/…`(404) → `/.netlify/functions/…` (L3 복구) |
| 3 | **차단**(라이브) | `netlify/functions/admin-ai-agent.ts` | 순직 도구 3개를 AI 비서 도구 묶음에 등록(키워드 라우팅) — 일반 검색 격리 불변 (L4 복구) |
| 4 | 품질/잠재 | `lib/martyrdom-ai.ts` | 발간 자체조사에서 사건 메타(`case_no·outcome·case_kind`)를 **잘못된 테이블에서 조회**(SQL 오류) → 사건 테이블 JOIN으로 수정. 현재 화면은 사건 미지정으로 호출해 미발현이나, 사례연구(사건 지정) 경로에서 깨짐 |
| 5 | 품질 | `netlify/functions/admin-martyrdom-publication-export.ts` | HTML 내보내기가 조립본(charset·면책) 대신 본문 원본만 인코딩 → 조립본 인코딩으로 수정 |
| 6 | 품질 | `admin-martyrdom-stats.ts` + `lib/martyrdom-ai.ts` | 유형별 집계 묶음 기준을 표시값과 일치(`GROUP BY COALESCE…`) → "미분류" 중복 막대 제거 |
| 7 | 권한(Swain ⓐ) | `netlify/functions/admin-martyrdom-publication.ts` | 발간 쓰기(생성·상태변경·발간·삭제)를 **admin 이상**으로 정합(`requireRole(member,"admin")`) — 운영자는 조회·내보내기만. 기존 PATCH/DELETE의 super_admin 단독을 admin+로 통일, POST에 게이트 신설 |
| 8 | 권한(Swain ⓑ) | `lib/ai-agent-config.ts` | AI 비서 권한 판정(`isRoleAllowed`)을 등급제(super_admin>admin>operator)로 교정 — `required_role='operator'` 도구를 운영자·admin·super_admin 모두 사용 가능(이전엔 'admin'을 과잉 차단). 비표준 역할값은 기존 규칙 유지(동작 불변) |

전부 `npx tsc --noEmit` 통과.

---

## 5. 권한 정책 — Swain 결정 2건 반영·수정 완료 (2026-05-27)

**(ⓐ) 연구 발간 권한 단계화** — Swain 결정: **쓰기는 admin 이상, 운영자는 조회만.**
- GET(목록·상세 조회): 운영자 이상 그대로(`requireAdmin`) — 운영자도 발간물 열람 가능.
- POST(생성)·PATCH(상태변경·발간)·DELETE: **admin 이상**으로 통일(`requireRole(member,"admin")` → 운영자 차단). 기존엔 PATCH/DELETE만 super_admin 단독, POST는 운영자 레벨로 제각각이던 것을 admin+로 일관화.
- publication-export(내보내기): 조회·출력 성격 → 운영자 이상 유지(이미 GET 상세로 본문 열람 가능하므로 정합). publication-generate-background는 내부 시크릿 게이트(INTERNAL)라 권한 변경 불필요.
- (※ 권한 정책 관리 UI 컨트롤은 메인이 라운드 종결 단계에서 처리 — C는 코드 게이트만.)

**(ⓑ) AI 순직 도구 권한 — 운영자가 쓰게 교정** — Swain 결정: **운영자·admin·super_admin 모두 3개 도구 사용 가능.**
- 원인: 권한 판정 함수(`isRoleAllowed`)가 super_admin/admin/없음 계층만 알아 `required_role='operator'`인 도구에서 **'admin'을 과잉 차단**(격리 누수 아님).
- 선택: **판정 로직 교정(등급제)** 채택 — DB 시드(`required_role='operator'`)는 그대로 두고 `isRoleAllowed`를 등급제(super_admin>admin>operator)로 고침. 1회용 마이그 불필요·재배포 외 라이브 액션 없음·`required_role='operator'`=‘운영자 이상’ 의미가 정확·기존 `ROLE_RANK` 패턴과 정합. (시드값 변경 마이그 방식은 라이브 호출 1회 추가가 필요해 비채택.)
- 비표준 역할값은 기존 규칙 유지(동작 불변). 핸들러 `ensureRole(["operator","admin"])`는 이미 정합. 격리 불변(순직 테이블 직접 조회·일반 검색 `qna·manual` 유지).
- 영향: `required_role='operator'` 도구는 현재 순직 3개뿐 → 다른 도구 동작 변화 없음. 운영자/admin/super_admin 모두 정상 사용.

---

## 6. 메인 후속 (배포·재검증)

1. 검증 브랜치(`verify/martyrdom-p4`) 머지 → **배포 1회**.
2. 배포 후 라이브 재검증: **L1 유족요약 생성**(쉬운 말 요약·다음 할 일) / **L3 발간 본문 실제 생성**(자체+AI 블렌드·비식별화·reidRisk) / **L4 AI 비서 "진행 중 순직 사건 알려줘" → 사건 목록 응답**.
3. 권한 ⓐ·ⓑ는 Swain 결정대로 코드 반영 완료(재시드 불필요) — 배포 후 확인: **운영자 계정으로 발간 생성 차단·조회 허용 / admin 계정으로 AI 순직 도구 사용 가능**. 권한 정책 관리 UI 컨트롤은 메인 종결 단계 처리.
4. 라운드 마감 체크리스트(§P4.7): 메뉴얼·knowledge.md·AI 도구 카탈로그 동기화 + 딥릴리프 전체 설계서 → milestone 이동.

---

## 7. 라이브 흔적 (정리 완료)

- 로그인 쿠키: 리포 밖 임시 파일에 저장 후 **검증 종료 시 삭제**(커밋·기재 안 함).
- 테스트 발간물 1건(id=1) 생성 → **삭제 완료**(현재 발간물 0건).
- 유족요약은 500으로 저장 실패해 잔여 행 없음.
- AI 비서 대화 로그 수 건 생성(읽기 전용 로그·무해·잔존).
