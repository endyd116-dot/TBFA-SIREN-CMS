# 2026-06-03 야간 자율 검증 보고서 (메인 직접 검증)

> Swain 지시: "운영자 계정으로 검증 + 오늘 수정분 전부 메인이 직접 검증(검수·라이브·연관성·메뉴 통합흡수). 자러 가니 권한 묻지 말고 알아서 수정 후 보고서만."
> 검증 계정: 슈퍼어드민 admin / 운영자 endy0718@naver.com(박새로이, id=5, role=operator)
> 방식: 코드 정합 정독 → 라이브 호출(슈퍼+운영자 2계정) → 발견 즉시 fix → 배포 → 재검증

---

## 0. 한 줄 결론

**오늘 수정분 7개 영역 전부 정상 작동 확인.** 코드 정합 + 라이브 모두 통과. 검증 중 ① 근무형태 흡수 잔재 죽은코드 178줄 제거 ② AI 발송 요청에 매뉴얼 RAG 주입 생략(하드닝) 2건을 추가 수정·배포. **AI 비서는 처음부터 정상이었고**, 검증 중 보였던 "도구 미실행"은 전부 **검증 도구(curl)의 한글 인코딩 깨짐**이 원인이었음(브라우저 UI는 정상 UTF-8 전송).

---

## 1. 검증 영역별 결과

### ① 급여 일급제 (PASS)
- **계산 로직**(payroll-calc.ts): 일급 = (연봉÷12) ÷ 그달 영업일수(월~금, 공휴일 포함), 기본급 = (출근일+유급휴가일)×일급. 야근=0, 무급일은 출근일에서 자동 제외. 정합.
- **라이브 수치 검증**: 김광일(연봉 3,500만) 2026-05 명세서 — 월급 2,916,667 ÷ 5월 영업일 21일 = 일급 **138,888.89**, 출근 1일 → 기본급 138,888.89, 4대보험 공제 후 실수령 **125,827.67**. 수식 완벽 일치.
- **재집계 워크플로우**: 5·6월 비강제 재집계 — 후보 2명(연봉 설정자), 오류 0. "재집계 안됨"의 원인은 **직원 연봉 미설정**이었고, 직원연봉 설정 화면 신설로 해소. (현재 김광일 3,500만·지주은 2,500만 설정됨, 나머지 2명 0원이라 후보 제외 — 정상.)
- **명세서 열 정합**: 목록 헤더 9개 = 행 셀 9개 정확 일치(이름·직책·근무일·기본급(출근일)·성과·세전·실수령·상태·동작). 야근(h)·차감 열 제거로 "숫자 밀림" 해소 확인. 상세·PDF·CSV에서도 야근 전부 제거됨.

### ② 권한 게이트 (PASS)
- **백엔드 게이트 라이브**(운영자 계정): 콘텐츠 6종(추모관·기관소식·카카오템플릿·유족스토리·발송템플릿·자동발송) 전부 **403 차단**. 슈퍼어드민은 전부 200 통과. 미게이트 엔드포인트는 운영자 200 허용. fail-closed(미등록 키=운영자 자동 차단) 확인.
- **급여·근태설정 슈퍼어드민 전용**: 운영자 급여 403, 슈퍼 200.
- **권한 매트릭스 완비**: role_permissions 57키 등록. 사이드바 MENU_PERM·백엔드 PATH_FEATURE의 모든 참조 키가 57키 안에 존재 — **누락 시드 0**. (R46-1 시드로 이미 해소됨.)
- **사이드바 게이팅 로직**: cms-tbfa.js `_permAllows`가 백엔드 canAccess와 동일 규칙. 권한 매트릭스 GET은 운영자도 200(사이드바 가림용 읽기, 쓰기는 슈퍼 전용) — 의도된 동작.

### ③ AI 비서 도구·모델 (PASS — 코드 정상)
- **라이브 도구 호출 7/7 정상**(UTF-8): 회원검색·후원통계·문자(sms_send)·이메일(email_send)·작업목록·회원통계·후원내역. dry-run 프리뷰·승인 대기까지 정상.
- **외부 주소·번호 직접 발송**: email_send(toEmails)·sms_send(toPhones) 정상 호출·dry-run.
- **모델 체인**: HIGH=gemini-3.5-flash(실존 확인) 1순위, LOW=gemini-2.5-flash. 가용 모델 31종 조회 — 체인 전 모델 실존(404 폴백 낭비 없음).
- **★ 검증 중 오해와 정정**: curl 테스트에서 이메일 도구가 간헐적으로 안 불리는 현상 → 진단 코드 임시 삽입 후 추적 결과, **서버에 도착한 메시지가 모지바케**(`제목`→`����`)였음. Git Bash(Windows) curl이 `-d` 한글을 UTF-8로 못 보낸 것. 깨진 바이트가 우연히 재정(finance) 그룹 키워드와 매칭→email_send 미로드. node UTF-8 페이로드로 재검증하니 즉시 정상. **코드·모델 무결, 브라우저 UI 영향 없음.**

### ④ 알림 통합 (PASS)
- **UNION**(admin-workspace-notifications): workspace_notifications + notifications 두 테이블을 한 피드로. unreadCount·total 양쪽 합산, "모두읽음"이 양쪽 갱신. 벨 배지는 poll() 재조회로 즉시 0 → "+99 안 사라짐" 해소.
- **라이브**: 운영자 알림 GET 정상(미읽음 67/총 70).

### ⑤ 로그아웃 (PASS)
- 통합 CMS(cms-tbfa) 우상단 #cmsLogout + 핸들러, 워크스페이스 알림·성과관리 페이지 사이드바 로그아웃 추가.
- `/api/auth/logout`이 사용자(siren_token)·관리자(siren_admin_token) 쿠키 둘 다 삭제 → CMS 로그아웃 실효 확인.

### ⑥ 근무형태 관리 흡수 (PASS + 정리)
- 근무형태 관리 탭 제거 → 직원 스케줄(schedule) 탭이 흡수(기본 근무형태+요일별 HYBRID+거점). initScheduleTab는 자체 함수만 사용, 흡수된 옛 코드와 의존성 0.
- **추가 수정**: 호출되지 않는 죽은 코드(initWorkModesTab 외 178줄) 물리 제거 → 흡수 완성. node --check 통과, 페이지 동작 무변경.

### ⑦ 공휴일 시드 (PASS)
- 2026년 공휴일 20일 등재(설·추석·대체공휴일 포함). 무급 처리는 일급제 모델이 자동(미출근일=지급일 제외)이라 별도 플래그 불필요.

---

## 2. 이번 세션 추가 수정·배포

| 커밋 | 내용 | 위험도 |
|---|---|---|
| `ce5aec6` | 근무형태 흡수 잔재 죽은 코드 178줄 제거 | 낮음(미호출 코드·문법검사 통과) |
| `199078a` | AI 발송·생성 등 행동(HIGH intent) 요청에 매뉴얼 RAG 주입 생략 | 낮음(행동요청은 도구가 처리·정보성 질문엔 RAG 유지) |
| `433402f`·`dbb4e1c` | (기존 미푸시분) 야근 제거 + 로그아웃 — 함께 배포 | - |
| `d990057`·`31a3e4b`·`2a91a41` | 임시 진단 삽입→확장→**제거**(원인 규명용, 최종 클린) | - |

**DB 변경(비코드)**: AI 시스템 프롬프트에 "직접 발송 권한(운영자 요청 시 email_send/sms_send 즉시 호출, 메뉴로 떠넘기지 말 것)" 지침 추가(/api/admin-ai-config로 저장). 발송 요청 시 AI가 더 행동지향적으로 동작.

---

## 3. 발견했으나 수정 안 한 것 (정보·권고)

- **att-workmode-change-review 등 일부 att 엔드포인트**: PATH_FEATURE 미등록이라 att_manage 키 게이트는 안 받지만, att_manage가 operator:true라 결과는 정책과 일치. 운영자 차단이 필요하면 권한정책관리에서 att_manage operator 토글 OFF + 해당 엔드포인트 PATH_FEATURE 등록 권장(현재는 정책상 허용이 맞아 보류).
- **sms_send의 buildNextSteps("email_send")**: 동작무관 cosmetic(발송 후 인앱알림 제안). 변경 시 오히려 퇴보라 유지.

---

## 4. 회귀·연관성 점검

- 급여 일급제 ↔ 근태(출근일·유급/무급휴가)·성과(분기 정산) 연동 정합. 명세서 PDF·CSV·일괄발송·승인 흐름 야근 제거 반영.
- 권한 게이트 ↔ 사이드바(프런트)·백엔드(canAccess) 동일 규칙. 슈퍼어드민 항상 통과.
- 알림 두 시스템 → 한 벨·한 화면. RAG 격리(순직 martyr_* 제외) 유지.
- tsc 전체 통과. AI 진단 코드 완전 제거(누출 0 확인).

---

## 5. Swain 확인 권장(아침)

1. 브라우저로 통합 CMS 로그인 → AI 비서에 "○○@메일 / ○○번호로 ~ 보내줘" → dry-run 미리보기 뜨고 [승인] 시 실발송 되는지 한 번 체감.
2. 급여관리 → 5월 강제 재집계 → 김광일 명세서 일급 산정 줄(영업일/일급/지급일/미산입) 확인.
3. 운영자 계정 로그인 시 급여·권한정책·근태설정 등 메뉴가 사이드바에서 가려지는지.
4. 통합 CMS·워크스페이스 우상단/사이드바 로그아웃 동작.

— 메인(자율 검증) 2026-06-03
