# R32-P0 Before/After 라이브 검증 보고서

> 검증자: C 채팅 (Opus 4.7)
> 시각: 2026-05-19 (KST)
> 브랜치: verify/r32-p0 (main 기반)
> 검증 대상: [docs/diagnostics/2026-05-19-integrated-r32-candidate.md](../diagnostics/2026-05-19-integrated-r32-candidate.md) §1 Critical 6건

---

## §0 검증 환경

| 항목 | Before | After |
|---|---|---|
| main 커밋 | **5aa5935** (R32 후보 진단서 안착) | (메인 R32-P0 머지 후) |
| 라이브 URL | https://tbfa.co.kr | (동일) |
| 어드민 계정 | admin / (비번은 문서에 기재하지 않음) | (동일) |

---

## §1 Before — fix 전 운영 깨짐 재현 (main @ 5aa5935)

| # | 항목 | 재현 결과 | 응답·에러 | 정적 근거 |
|---|---|---|---|---|
| B1 | C1 비매출 select | ❌ **HTTP 404 (HTML 페이지)** | 백엔드 라우트 부재 — JS는 `/api/milestone-nonrevenue/select` POST 호출하나 `milestone-nonrevenue.ts:7`의 config는 `/api/milestone-nonrevenue` 정확 매칭만 (Netlify segment 추가 미허용) | `workspace-milestones.js:839` 호출 vs `milestone-nonrevenue.ts:104` 액션 분기. sql.raw BUG는 라우팅 도달 전에 차단되어 보이지 않음 (잠재 — fix 후 라우팅 잡히면 즉시 발현) |
| B2 | C2 분기 PATCH | ❌ **HTTP 500** `there is no parameter $2` | drizzle `sql.raw(query, params)` 두 번째 인자 무시 — `$1`·`$2` placeholder가 PostgreSQL에 그대로 전달 | `milestone-quarters.ts:70` `db.execute(sql.raw('UPDATE quarters SET ... WHERE id = $${vals.length}', vals))` |
| B3 | C3 정의 PATCH | ❌ **HTTP 405 / 400** (라우팅 + sql.raw 이중 깨짐) | `PATCH /api/milestone-definitions/1` segment → Netlify 매칭 안 됨 405. `PATCH ?id=1` 쿼리 → "ID 없음" 400 (`pathname.split("/").pop()`이 함수명을 반환). sql.raw BUG는 잠재 (fix 후 발현) | `milestone-definitions.ts:84·104` |
| B4 | C4 CSV 급여 export | ❌ **HTTP 500** `column m.base_salary does not exist` | `members` 테이블에 `base_salary` 컬럼 부재 (schema grep 결과 0건) | `admin-milestone-settlement-export.ts:36` `SELECT m.base_salary`. 라이브 호출 `/api/admin/milestone-settlement-export?quarterId=1` |
| B5 | C5 직원 스케줄 | ❌ **HTTP 404 (HTML 페이지)** | 백엔드 함수 `admin-att-member-schedule.ts` 부재 — 존재하는 함수는 `admin-att-schedules.ts`·`admin-att-schedule-override.ts` | `admin-workspace-management.js:250·265` `/api/admin-att-member-schedule` 호출 |
| B6 | C6 정책 변경 무효 | ❌ **HTTP 200이지만 DB 미반영** | JS body 키 `checkinTime`·`checkoutTime`·`lateGraceMinutes`·`earlyLeaveMinutes`·`overtimeAfterHours`·`annualLeaveDays`·`workDaysPerWeek`·`gpsCheckEnabled` ↔ 서버 응답 키 `checkInTime`·`checkOutTime`·`lateGraceMins`·`earlyLeaveGraceMins`·`dailyHours`·`breakMins`·`weeklyMaxHours`·`coreStartTime` 등 **전체 불일치**. PUT 호출 후 GET 시 `checkInTime: "09:00:00"` 그대로 — 변경 사항 0건 적용 | `admin-workspace-management.js:474~514` body vs `admin-att-policy` GET 응답 키 매핑 표 §1.1 |

### §1.1 B6 키 매핑 표 (운영 깨짐 명세)

| JS 송신 키 (savePolicy) | 서버 응답 키 (admin-att-policy GET) | 일치 |
|---|---|---|
| `checkinTime` | `checkInTime` (대문자 I) | ❌ |
| `checkoutTime` | `checkOutTime` (대문자 O) | ❌ |
| `lateGraceMinutes` | `lateGraceMins` | ❌ |
| `earlyLeaveMinutes` | `earlyLeaveGraceMins` | ❌ |
| `overtimeAfterHours` | `dailyHours`/`weeklyMaxHours` | ❌ (개념도 다름) |
| `annualLeaveDays` | (서버 응답 없음 — 별도 테이블) | ❌ |
| `workDaysPerWeek` | (서버 응답 없음) | ❌ |
| `gpsCheckEnabled` | (서버 응답 없음 — 다른 키) | ❌ |

→ **8/8 키 모두 불일치**. PUT 성공 응답 받아도 서버가 모든 필드를 undefined로 인식 → 기존값 유지. 어드민이 정책 변경 시도 시 화면상 성공 토스트만 보고 실제로는 0건 반영.

---

## §2 Before 종합

| 항목 | Before 결과 |
|---|---|
| 운영 깨짐 재현 성공률 | **6 / 6 (100%)** |
| HTTP 500 | B2·B4 (2건 — 직접 에러 노출) |
| HTTP 404 | B1·B5 (2건 — 라우팅 자체 부재) |
| HTTP 405/400 | B3 (1건 — 라우팅 + 키 모두 깨짐) |
| 200 위장 (사일런트 실패) | B6 (1건 — 가장 위험: 어드민이 성공으로 오인) |

진단서 §1 Critical 6건 모두 **운영 환경에서 실제 재현** 확인. fix 즉시 필요.

---

## §3 After — fix 후 동일 시나리오 (main @ afff8e7 + base_salary 마이그 완료 f1c7450)

| # | 항목 | 결과 | 응답·근거 | 비고 |
|---|---|---|---|---|
| A1 | C1 비매출 select | ⚠️ **PARTIAL** | `POST /api/milestone-nonrevenue` body `{action:"select",...}` → 400 "필수 필드 누락" (action 분기 미적용, milestone-nonrevenue 액션 분기로 빠짐). `POST /api/milestone-nonrevenue/select` segment → **404 HTML 페이지** | sql.raw fix는 코드에 적용됨(line 117) — 단 Netlify config.path `/api/milestone-nonrevenue` 정확 매칭이 segment 라우팅을 막아 fix 도달 불가. **운영 깨짐 유지** |
| A2 | C2 분기 PATCH | ✅ **PASS** | `PATCH /api/milestone-quarters?id=1` body `{status:"SETTLED"}` → 200 + `{quarter:{...status:"SETTLED"...}}`. sql 템플릿 합성 정합 | fix 코드가 `searchParams.get("id") \|\| pathname.pop()` fallback 추가로 쿼리 라우팅 동작 |
| A3 | C3 정의 PATCH | ❌ **FAIL 유지** | `PATCH /api/milestone-definitions?id=1` → "ID 없음" 400 (코드가 `pathname.pop()`만 사용, `searchParams` fallback 없음). `PATCH /api/milestone-definitions/1` → 405 (Netlify segment 라우팅 안 됨) | sql.raw fix는 적용됐으나 라우팅 도달 불가. 단순 fix(searchParams fallback 1줄) 또는 config 와일드카드 필요 |
| A4 | C4 CSV 급여 | ✅ **PASS** | `GET /api/admin/milestone-settlement-export?quarterId=1` → 200 + CSV 헤더 `직원명,직책,매출연동인센티브,비매출보너스,총변동급,기본연봉,상태,지급일` | base_salary 마이그(f1c7450) 후 schema 활성화. 한글 라벨 정합 |
| A5 | C5 스케줄 | ✅ **PASS** | `POST /api/admin-att-schedules` body `{memberUid:"1",workMode:"OFFICE",startDate:"2026-05-19",endDate:"2026-05-19"}` → 201 + 생성됨. `GET ?memberUid=1` → 200 + 1건 반영 | 경로 정정(`admin-att-member-schedule` → `admin-att-schedules`) + body 키 정합 동작 |
| A6 | C6 정책 변경 | ✅ **PASS** | `PUT /api/admin-att-policy` body `{checkInTime:"09:30:00",lateGraceMins:15}` → 200. `GET` 재확인 → `09:30:00` / `15` **실제 반영**. 원복(`09:00`/`10`) → 200 + 09:00:00/10 정상 복귀 | **사일런트 실패 해결 — Before B6의 가장 위험한 케이스 fix 확정** |

---

## §4 회귀 점검 — 14건 ALL PASS

| # | 항목 | 결과 |
|---|---|---|
| 1 | admin-att-correction-review (P1 Q2) | ✅ 200 |
| 2 | admin-att-leave-review (P1 Q3) | ✅ 200 |
| 3 | admin-att-leave-balances (P1 Q4) | ✅ 200 |
| 4 | admin-milestone-settlement (P2 BUG fix) | ✅ 200 |
| 5 | milestone-revenue (P2 BUG fix) | ✅ 200 |
| 6 | admin-milestone-revenue | ✅ 200 |
| 7 | milestone-definitions GET | ✅ 200 |
| 8 | milestone-quarters GET | ✅ 200 |
| 9 | admin/me | ✅ 200 |
| 10 | att-leave-types | ✅ 200 |
| 11 | admin-att-policy (R32 fix 영역) | ✅ 200 |
| 12 | Permissions-Policy geolocation=(self) | ✅ |
| 13 | workspace-attendance.js?v=6 (R30 캐시버스터) | ✅ |
| 14 | lib-kst.js?v=1 로드 (R30 KST) | ✅ |

**R29 P1·P2 + R30 KST 모두 무회귀 확정.**

---

## §5 종합 평가

### 핵심 결과
- **Before 6/6 깨짐 → After 4/6 완전 PASS + 2/6 PARTIAL/FAIL** (A1·A3 라우팅 한계)
- **회귀 0** — R29·R30 영역 전체 무영향
- **B6 사일런트 실패(최고 위험) 완전 해결** — 어드민 정책 변경이 실제 DB 반영 확인

### 미해결 BUG (R32-P0-B 후속 fix 필요)

| # | 함수 | 문제 | 권장 fix |
|---|---|---|---|
| FIX-1 | `milestone-definitions.ts` PATCH·DELETE (line 85·114) | `pathname.pop()`만 사용 → `?id=` 쿼리 미수령 + Netlify segment 라우팅 불가 | 한 줄 추가: `const id = url.searchParams.get("id") \|\| url.pathname.split("/").pop()` (A2 fix와 동일 패턴). 또는 `config.path` 와일드카드 `["/api/milestone-definitions", "/api/milestone-definitions/*"]` |
| FIX-2 | `milestone-nonrevenue.ts` POST select (line 104) | `action === "select"` 분기가 `pathname.pop()`을 통해 결정되나 Netlify segment 미라우팅 → body 호출 시 `milestone-nonrevenue` 액션 분기로 빠져 다른 필드 검증 | 옵션 A: `config.path` 와일드카드. 옵션 B: body에 `action` 키 우선 분기 (`const action = body?.action \|\| pathParts.pop()`) |

### Netlify config.path 와일드카드 사례 (참고)
프로젝트 내 이미 사용 중: `admin-grades.ts:169 path:"/api/admin/grades*"`·`admin-pending-approvals.ts:395 path:"/api/admin/pending-approvals*"`. 동일 패턴 적용 가능.

### 자율 fix 보류 사유
FIX-1·FIX-2 모두 B 영역(성과) 코드 변경. 트리거 §8 "A·B 영역과 무관한 단순 fix면 자율"이라 B 영역은 메인/B 결정. 본 verify 브랜치에는 보고만 동봉.

### 권장 액션
1. **🔴 즉시**: 메인이 FIX-1(한 줄 추가) 즉시 적용 → 어드민 정의 수정 복구
2. **🟡 단기**: FIX-2 결정(config 와일드카드 vs body action 우선) — B 검토 후 적용
3. **🟢 후속**: R32-P0의 segment 라우팅 패턴 전반 점검 — `admin-milestone-settlement.ts:23·24` 등 동일 패턴 함수의 라우팅 가능성 확인 (이번엔 라이브 PUT 호출 안 해봤음)
